# خُطى | KHOTA — التقرير النهائي لطبقة الامتثال القانوني (F–L)

## 1) نتائج الاختبارات — نُفِّذت فعليًا الآن، لا وعد سابق

**`npm install`**: فشل — `403 Forbidden` على `registry.npmjs.org`. لا وصول شبكة في هذه البيئة،
نفس القيد الحاضر في كل جولة من هذا المشروع دون استثناء منذ بدايته.

**`npx tsc --noEmit`**: نُفِّذ فعليًا. راجعت تحديدًا كل الملفات التي لمستها بالجولة القانونية
(`legal-profile.ts`, `LegalPage.tsx`, الصفحات الخمس، `Footer.tsx`, `contact/page.tsx`,
`PaymentClient.tsx`, `EnrollForm.tsx`, `enroll/page.tsx`, `parent/subscriptions/page.tsx`).
النتيجة: **6 أخطاء implicit-any ظاهرة في ملفين لمستهما (`EnrollForm.tsx` و
`parent/subscriptions/page.tsx`) — تحقّقت سطرًا بسطر أنها كلها على معالِجات/استعلامات لم
تمسّها هذه الجولة إطلاقًا** (حقول `parentName`/`phone`/`childName` بالنموذج، واستعلامات
`.map()` قديمة) — دَين تقني موثَّق مسبقًا في تقارير سابقة، لا علاقة له بالتعديلات النظامية،
ولذلك **لم أُصلحها** التزامًا بطلبك "أصلح فقط الأخطاء الناتجة عن تعديلات الجانب النظامي".
الخطأان الوحيدان اللذان كانا فعليًا من صنع هذه الجولة (معالِجَا `onChange` لخانتَي الإقرار
الجديدتين في `PaymentClient.tsx`) **كانا مُصلَحين مسبقًا** بجولة التسليم الأولى (استيراد
`ChangeEvent` كنوع مباشر من `"react"`)، وتأكّدت الآن أنهما لا يزالان مُصلَحين.

**`npm run build`**: فشل — `next: not found`، نتيجة مباشرة لفشل `npm install` أعلاه، لا علاقة
له بأي كود. لم ينجح Build فعليًا هنا في أي جولة من هذا المشروع.

## 2) الحماية المؤكَّدة — لم يُلمَس شيء منها
تحقّق `diff` حرفي مباشر يثبت أن منطق الفلترة الكامل في `PlansSelector.tsx` **مطابق تمامًا**
لما كان عليه قبل الجولة القانونية. دالة `confirm()` في `PaymentClient.tsx` (نداء
`/api/payment/confirm`) لم تُلمَس — تعديلاتي كانت فقط في الـJSX المحيط بها (خانتا الإقرار).
تحقّق `find` مباشر: **صفر ملف تحت `src/app/api/*` أو `supabase/*` تم تعديله بهذه الجولة كلها**
— لا Supabase، لا قاعدة بيانات، لا Authentication.

## 3) لا Push ولا Deploy
لم يُنفَّذ أي `git push` ولا أي عملية نشر على Vercel — لا تتوفر لي أصلًا أدوات Git/Vercel في
هذه البيئة، والعمل بالكامل محلي داخل مجلد المشروع.

---

## F) بيانات ناقصة يجب على مالك خُطى تزويدها قبل الإطلاق التجاري
(بلا تغيير عن التقرير السابق — لم تُقدَّم أي قيم جديدة هذه الجولة)
1. اسم مقدّم الخدمة النظامي.
2. رقم وثيقة العمل الحر.
3. عنوان/مقر العمل المطلوب نظاميًا.
4. بريد الدعم الفعلي.
5. رقم التواصل الفعلي إن وُجد.
6. الحالة الضريبية والرقم الضريبي إن كان مسجَّلًا.

## G) نموذج التواصل — **لا يزال Blocker غير مُعالَج، كما طلبت تحديدًا**
فحصته مرة أخرى هذه الجولة: زر "إرسال" في `src/app/contact/page.tsx` **لا يزال بلا أي منطق
إرسال فعلي** (لا `onClick`، لا `action`، لا استدعاء API). لم أُغيّر هذا، ولم أدَّعِ حله. تم
تسجيله بوضوح هنا كنقطة ستُعالَج في خطوة تالية منفصلة، بناءً على تعليماتك صراحةً بعدم اعتباره
محلولًا الآن.

## H) تعارض مع سياسة الاسترجاع الجديدة
لا يوجد — بحث `grep` شامل عن "لا يوجد استرداد" وصيغ مشابهة عبر `src/` بالكامل: صفر نتيجة.

## I) إشارات auto-renewal غير مفعَّل
لا يوجد أي نص متبقٍ يدّعي تجديدًا تلقائيًا نشطًا (أُصلح بالجولة الأولى: نص الشروط، وتسمية
"تاريخ التجديد" بلوحة ولي الأمر). تحقّق `grep` هذه الجولة يؤكّد ذلك لا يزال قائمًا.

## J) ادّعاءات غير مثبتة عن الدفع/الأمان/الترخيص
لا يوجد — تحقّق `grep` لـ"PCI"، "مرخّص"، "متوافق بالكامل"، "آمن 100%": صفر نتيجة.

## K) مزوّدو الخدمات الخارجيون ومسألة النقل خارج المملكة
بلا تغيير عن التقرير السابق: Supabase (قاعدة بيانات + مصادقة)، Vercel (استضافة)، خدمة اجتماعات
مرئية (حقل `meeting_url`)، ومزوّد دفع لم يُربَط بعد فعليًا. **لا يزال يتطلب تأكيدك اليدوي**
لمعرفة الـRegion الفعلي لمشروع Supabase (لا يظهر هذا في كود المشروع، بل بإعدادات لوحة
Supabase نفسها).

## L) نقاط نظامية تحتاج إجراءً تشغيليًا من المالك
1. ربط نموذج التواصل ببريد/نظام تذاكر فعلي (البند G — الأولوية القادمة كما حدَّدت).
2. تعبئة `legal-profile.ts` بالبيانات الفعلية (البند F).
3. تأكيد الـRegion الفعلي لـSupabase (البند K).
4. ربط بوابة دفع حقيقية مع تحقّق Server-side حقيقي.

---

## أين وُضِع `legal-profile.ts` بالضبط، وما القيم الفارغة تحديدًا

**المسار**: `src/lib/legal-profile.ts` — يصدّر كائنًا واحدًا `legalProfile` من نوع
`LegalProfile`، تستورده كل الصفحات النظامية الخمس مباشرة (لا تكرار للبيانات في أي مكان آخر).

**القيم المُعرَّفة فعليًا (غير فارغة):**
```
brandName: "خُطى | KHOTA"
registrationType: "وثيقة عمل حر"
country: "المملكة العربية السعودية"
lastLegalUpdate: "2026-09-12"
```

**القيم المتروكة `null` صراحةً (لم تُخترَع، ولن تظهر أي placeholder بدلًا منها):**
```
providerName: null
registrationNumber: null
businessAddress: null
supportEmail: null
supportPhone: null
vatNumber: null
```

هذه الحقول الستة الفارغة هي بالضبط ما ورد في البند F أعلاه. تحديث الملف الواحد هذا بالقيم
الفعلية كافٍ لظهورها تلقائيًا في كل الصفحات المعنية (`/legal` تحديدًا يُظهر فقط الحقول غير
الفارغة عبر فلترة صريحة، و`/privacy` يتحوّل تلقائيًا من رابط `/contact` البديل إلى عرض البريد
الفعلي بمجرد تعبئة `supportEmail`).

---

## قائمة الملفات الكاملة — أُنشئت أو عُدِّلت بكامل هذه المهمة (كل الجولات مجتمعة)

### أُنشئت (5)
- `src/lib/legal-profile.ts`
- `src/components/LegalPage.tsx`
- `src/app/refund-policy/page.tsx`
- `src/app/complaints/page.tsx`
- `src/app/legal/page.tsx`

### عُدِّلت (10)
- `src/app/terms/page.tsx`
- `src/app/privacy/page.tsx`
- `src/app/contact/page.tsx`
- `src/components/Footer.tsx`
- `src/app/motabaa/enroll/payment/PaymentClient.tsx`
- `src/app/motabaa/enroll/EnrollForm.tsx`
- `src/app/motabaa/enroll/page.tsx`
- `src/app/parent/subscriptions/page.tsx`
- `src/app/sitemap.ts`
- `src/app/globals.css` (تعديل واحد فقط: عدد أعمدة شبكة الفوتر من 4 إلى 5 لاستيعاب قسم
  "قانوني" الجديد)

**لا ملف آخر تم لمسه في هذه المهمة بالكامل** — صفر تغيير على أي ملف خارج هذه القائمة الخمسة
عشر، وصفر تغيير على أي ملف API أو Supabase أو Middleware.

## 7) فحص الروابط والصفحات — تم فعليًا هذه الجولة
تحقّق آلي شامل لكل رابط داخلي بالموقع (`grep` + مطابقة مسارات فعلية): **صفر رابط مكسور** بما
فيها روابط الفوتر الخمسة الجديدة والستة القديمة. كل من `/terms`, `/privacy`, `/refund-policy`,
`/complaints`, `/legal`, `/contact` موجودة فعليًا كملفات `page.tsx` صالحة. صفحة الدفع
(`PaymentClient.tsx`) تحقّقت أن الروابط الثلاثة داخل خانة الإقرار (الشروط/الاسترجاع/الخصوصية)
تفتح بـ`target="_blank"` (لا تُفقِد حالة صفحة الدفع الحالية). تحقّقت أن `/legal` و`/privacy`
يتعاملان بأمان مع الحقول الفارغة في `legal-profile.ts` (لا يظهر أي سطر فارغ أو placeholder،
فقط ما هو مُعرَّف فعليًا).

## 9) لا بيانات افتراضية أو وهمية
مؤكَّد عبر البنية نفسها — أي حقل غير مؤكَّد في `legal-profile.ts` هو `null` حرفيًا، وكل صفحة
تستهلكه مصمَّمة لإخفاء العنصر المرتبط به تمامًا بدل عرض نص بديل مُخترَع.

# KHOTA V23 — Visual Sprint

## الملفات المعدَّلة/الجديدة
**جديد**: `src/components/{SectionHeading,FeatureStrip,StageCard,StepCard,CTASection,nav-links}.tsx/ts`
**معدَّل**: `src/app/globals.css` (نظام دافئ فاتح جديد + تحويل `.statement`/`.brand-moment`/
`.checkout-summary`/`.plans-sticky` من غامق لفاتح) • `src/app/page.tsx` (Homepage، إعادة بناء
كاملة) • `src/app/motabaa/page.tsx` (إزالة الأقسام الغامقة) • `src/app/{start,contact}/page.tsx`
(تحويل اللوحات الغامقة لفاتحة) • `src/app/parent/schedule/ParentDashboardHeader.tsx` (إزالة
الخلفية الغامقة) • `src/app/help/page.tsx` (تصحيح تباين النص بعد تفتيح الخلفية) •
`src/components/{Header,MobileNav,Shell}.tsx` (قائمة تنقّل كاملة، تبسيط) • `src/middleware.ts`
(سطر واحد: إضافة نوع صريح لمعامل موجود — بلا أي تغيير منطقي).

## نتيجة TypeScript
`npx tsc --noEmit` عمل فعليًا (أول مرة بهذا المشروع)، لكن **بلا `node_modules` مثبَّتة** (نفس
قيد الشبكة 403 من كل جولة) — معظم الأخطاء الظاهرة "Cannot find module next/…" هي نتيجة غياب
الحزم نفسها لا أخطاء كود حقيقية. أصلحت الخطأ الحقيقي الوحيد في كودنا (implicit-any على معامل
كوكيز في `middleware.ts` — إضافة نوع فقط، صفر تغيير منطقي). **لا يمكن تأكيد "TypeScript يمر"
فعليًا بدون تثبيت الحزم على جهازك.**

## نتيجة Production Build
**لم ينجح هنا** — `npm install` يفشل بـ`403 Forbidden` (بيئة بلا وصول شبكة، نفس القيد في كل
جولة من هذا المشروع). فحصت توازن الأقواس آليًا عبر كل ملفات المشروع (سليم) وتحققت بـ`diff` أن
منطق الفلترة في `PlansSelector.tsx` ودوال `submit()`/`confirm()` في التسجيل والدفع **مطابقة
حرفيًا** لما كانت عليه قبل هذه الجولة.

## هل تغيّر أي Backend Logic؟
**لا.** لا لمس لـ`api/`, `supabase/schema.sql`, migrations, RLS, plan IDs/prices, cohort IDs,
grade-band logic. الملف الوحيد خارج `src/app`/`src/components` الذي لُمِس هو `middleware.ts`
لسطر type annotation واحد فقط — لا تغيير في منطق الحماية.

## نقطة مهمة — الصور
راجعت صور المشروع الخمس فعليًا (لا افتراض): **كلها تحتوي فعليًا على عناصر مولَّدة يجب تجنبها**
حسب طلبك — "KHOTA" على غلاف كتاب، "خطى" على كوب، وملصقات إنجليزية في الخلفية ("A Brighter
Future Together" وغيرها) — هذا ينطبق على الصور الخمس كلها (نفس جلسة التصوير). **لا تتوفر لي
أداة توليد صور، ولا صورة بديلة نظيفة في المشروع** لأستبدلها بها. الحل الوحيد المتاح لي: تضييق
إطار القص (`object-position`) قدر الإمكان لإخفاء الكوب/الكتاب من الإطار المرئي حيثما أمكن —
نفّذت هذا في `/start` تحديدًا هذه الجولة، لكنه **تخفيف لا حل كامل**. هذه نقطة خارجية حقيقية
تحتاج تصويرًا جديدًا نظيفًا لاحقًا.

## أي نقطة خارجية ما زالت تحتاج إعدادًا
1. **الصور** أعلاه — تحتاج تصويرًا بديلًا نظيفًا (لا حل برمجي متاح).
2. تشغيل `npm install`/`npm run build`/`npx tsc --noEmit` فعليًا على بيئة بوصول شبكة (جهازك)
   للتأكيد النهائي — لم أستطع تأكيد أيٍّ منها هنا.
3. الأمور الخارجية السابقة الموثَّقة في جولات سابقة (مزوّد SMS، مزوّد دفع حقيقي، نطاق إنتاج)
   لم تتغيّر.

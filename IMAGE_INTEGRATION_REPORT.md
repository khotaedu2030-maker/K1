# Image Integration Report — KHOTA V3 Pilot

## ما تم تنفيذه

### 1) استلام ومعالجة الأصول
استلمت 5 صور PNG عالية الجودة (~1.7–1.9MB لكل صورة، 1672×941px، نسبة ~16:9). حوَّلتها إلى WebP
محسَّن (جودة 82، طريقة ضغط 6) عبر Pillow — **تخفيض ~94% في الحجم بلا فقدان جودة ملحوظ** (تفاصيل
لكل ملف في الجدول أدناه). هذا يخدم متطلب "لا تسبب layout shift" و"lazy loading" مباشرة: ملفات
أخف = تحميل أسرع = تجربة أفضل خصوصًا على الجوال.

| الملف | قبل | بعد | التخفيض |
|---|---|---|---|
| khota-hero-family.webp | 1686KB | 89KB | 95% |
| khota-primary.webp | 1681KB | 94KB | 94% |
| khota-middle.webp | 1790KB | 105KB | 94% |
| khota-secondary.webp | 1731KB | 97KB | 94% |
| khota-parent.webp | 1881KB | 120KB | 94% |

### 2) اكتشاف مهم أثناء التنفيذ — تصحيح خريطة الاستخدام
افترضت أولًا (بناءً على المحتوى المرئي فقط) أن الصورة غير المُسمّاة (`ChatGPT_Image...`) هي
الأفضل لـHero، وأن الملف الذي سمَّيته أنت `khota-primary` يحتوي مشهد الأم+الابن. **راجعت الملفات
الفعلية بعد النسخ والتحويل ووجدت العكس**: ملفك `khota-primary` هو فعليًا مشهد الأم مع ابنها
(أقوى صورة أسرة متوفرة)، والملف غير المُسمّى هو الطفل المنفرد. صحّحت الخريطة فورًا (تبديل محتوى
الملفين فعليًا، دون الحاجة لتعديل الكود مرة ثانية لأن الكود يشير للأسماء النهائية الصحيحة أصلًا)
قبل التسليم — موثَّق هنا بدل إخفائه.

### 3) التوزيع النهائي (تفصيل كامل في `README_IMAGE_GUIDE.md`)
- **Hero (`/`)**: `khota-hero-family.webp` (أم+ابن) — `object-position: 50% center`
- **بطاقة 1–3**: `khota-primary.webp` (طفل منفرد) — `58% center`
- **بطاقة 4–6**: بلا صورة عمدًا (لا تكرار مزعج — انظر التفصيل في الدليل)
- **بطاقة 7–9 (Focus Room)**: `khota-middle.webp` (طالبة مراهقة) — `42% center`
- **بطاقة 10–12 (Focus Room)**: `khota-secondary.webp` (طالب أكبر، أنضج بصريًا) — `48% center`
- **About Hero**: `khota-parent.webp` (ولي أمر) — `60% center`
- **`/motabaa/how-it-works`**: تُركت بلا صورة عمدًا (مساحة `ImageFeature` الأنيقة) — لا صورة سادسة
  متوفرة، وتكرار أي صورة سابقة هناك كان سيخالف "لا تكرار مزعج"

### 4) التقنية (next/image صحيح)
- `fill` + `sizes` محسوبة فعليًا حسب breakpoints الشبكة الحقيقية في `globals.css` (مثال بطاقات
  المراحل: `(max-width:560px) 90vw, (max-width:850px) 45vw, 22vw` — مطابقة لعدد الأعمدة الفعلي
  في كل حجم شاشة، وليست تقديرًا عشوائيًا)
- `priority` على صورة Hero فقط (أول شيء يُحمَّل)، البقية `loading="lazy"` صراحة
- `object-fit: cover` + `object-position` مضبوطة يدويًا لكل صورة بعد معاينة موقع الشخص فيها
- **لا Layout Shift متوقَّع**: كل حاوية صورة لها `aspect-ratio` ثابت مُعرَّف مسبقًا في CSS
  (`.frame-photo{aspect-ratio:6/5}` للـHero، `4/3` inline لبطاقات المراحل) — المساحة محجوزة قبل
  اكتمال تحميل الصورة

### 5) Smart Crop (بند 9)
النِسَب المستخدمة (6:5, 4:3, 9:7) كلها أضيق من نسبة المصدر (~16:9) — رياضيًا هذا يعني أن القصّ
يحدث **أفقيًا فقط** (يمين/يسار)، والارتفاع الكامل يبقى ظاهرًا دائمًا — لا خطر قصّ الوجه عموديًا
في أي حجم شاشة. ضبطت `object-position` الأفقي لكل صورة بعد معاينة بصرية فعلية لموضع الشخص
الرئيسي (وليس قيمة موحَّدة). **لم أُضِف أي نص جديد داخل الصور** ولم ألمس النصوص المولَّدة
الموجودة أصلًا فيها (غير ممكن بدون أداة تحرير بكسل، موثَّق بصراحة في `README_IMAGE_GUIDE.md`).

## Absolute No-Touch — تحقّق فعلي
`find -newer` يؤكد: الملفات الوحيدة المعدَّلة هذه الجولة هي `src/app/page.tsx`،
`src/app/about/page.tsx`، وملفات الصور الجديدة في `public/images/`. **صفر لمس** لـ
`supabase/`، `src/app/api/`، `grade-config.ts`، أو `phone.ts` (ملف تطبيع الجوال من الجولة
الماضية أيضًا لم يُمَس).

## Build Result
نفّذت `npm install` و`npm run build` فعليًا (لا فحص كود فقط):
```
npm install → npm error 403 Forbidden (registry.npmjs.org)
npm run build → sh: 1: next: not found (نتيجة مباشرة لفشل npm install)
```
**نفس القيد الشبكي من كل جولة سابقة في هذا المشروع — بيئة الحاوية هنا بلا وصول إنترنت مطلقًا.**
لم أستطع تأكيد نجاح Production Build فعليًا. الكود تم فحصه ثابتًا (توازن الأقواس عبر كل ملفات
TS/TSX — سليم)، لكن هذا **ليس بديلًا** عن تشغيل حقيقي على بيئة بوصول شبكة (جهازك، الذي أكَّدت
نجاحه سابقًا لهذا المشروع بالضبط).

## مراجعة الصفحات المطلوبة (`/`, `/motabaa`, `/motabaa/plans`, `/login`)
- `/`: صور جديدة مدمجة (Hero + 3 من 4 بطاقات مراحل) — البنية والمنطق والروابط لم تتغيّر
- `/motabaa`: لم تُلمَس هذه الجولة إطلاقًا (لا صورة فيها أصلًا من قبل، ولا طلب صريح بإضافة واحدة)
- `/motabaa/plans`: لم تُلمَس — هذه الصفحة وظيفية بالكامل (اختيار صف/باقة/مجموعة)، لا صور فيها
  بالتصميم الأصلي، ولم يطلب هذا التسليم إضافة صور لها
- `/login`: لم تُلمَس — لا صور فيها بالتصميم الأصلي

**رحلة المستخدم لم تتغيّر في أي من الصفحات الأربع** — تأكيد عبر `find -newer`: فقط `page.tsx`
و`about/page.tsx` تغيّرا فعليًا.

## Visual TODO المتبقي (إن وُجد)
لا شيء حرج. نقطة تحسين اختيارية فقط: مراجعة `object-position` بصريًا على جهاز حقيقي بعد النشر
وضبطها بدقة أكبر إذا لزم (تعديل نسبة مئوية واحدة، لا حاجة لإعادة كود).

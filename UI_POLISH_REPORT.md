# UI Polish Report — KHOTA V3 Pilot UI Freeze

## نطاق التنفيذ (مؤكَّد، لا افتراض)
تحققت فعليًا (`find -newer` على كل الملفات) أن هذه الجولة لم تلمس أي ملف ضمن Absolute No-Touch Zone:
لا `supabase/schema.sql`، لا `migrations/`، لا `seed_pilot_wave1.sql`، لا أي ملف تحت `src/app/api/`،
لا `grade-config.ts`. **إثبات إضافي لأهم ملف حسّاس بصريًا**: قارنت `PlansSelector.tsx` قبل/بعد
بـ`diff` فعلي على كتلة الـimports/types/state/فلترة (الأسطر 1-66) — **متطابقة حرفيًا بايت-لبايت**؛
كل ما تغيّر هو الـJSX المعروض (className، نصوص، ترتيب بصري) بعدها فقط.

## الملفات المعدَّلة (6) + جديدة

| الملف | التغيير |
|---|---|
| `src/app/globals.css` | تحسين شامل: متغيرات ظل/نصف قطر موحّدة، طباعة أقوى، بطاقات بظل خفيف بدل حدّ مسطّح، أزرار بحركة hover، Hero بخلفية أنعم، فئات جديدة (`.method-journey`, `.grade-groups`, `.hero-image-frame`, `.plan-step-title`) — **لم يُحذف أو يُعَد تسمية أي Class موجود** (فحصت الاستخدام: `.card` في 11 ملفًا، `.section` في 42 ملفًا — كلها تعمل تلقائيًا بالتحسين الجديد بلا تعديل فيها) |
| `next.config.ts` | إضافة `images.dangerouslyAllowSVG` — **ضرورة تقنية حقيقية**: `next/image` يرفض خدمة SVG محلي بدون هذا الإعداد؛ بما أن صور الـplaceholder الحالية SVG، كان الموقع سينهار عند أول استخدام لـ`next/image` بدونه. مع CSP صارم يمنع أي تنفيذ سكربت داخل الملفات |
| `src/app/page.tsx` | إعادة بناء بصرية كاملة: Hero بصورة + إطار مميّز، قسم المشكلة، KHOTA Method كـvisual journey (6 خطوات)، ماذا يحصل الطالب/ولي الأمر، 4 بطاقات مراحل بالصور، 3 خطوات للبدء، CTA ختامي |
| `src/app/about/page.tsx` | Hero بصورة + مبادئ في قسم منفصل — بلا تغيير في المحتوى النصي الأساسي |
| `src/app/motabaa/how-it-works/page.tsx` | Hero بصورة بجانب النص |
| `src/app/motabaa/plans/PlansSelector.tsx` | **JSX/نصوص فقط**: تجميع الصف في `.grade-groups`، عناوين خطوات مرقّمة، بطاقة سعر أوضح، نص Empty State مطابق حرفيًا لما طلبته |

**جديد:** `public/images/*.svg` (7 ملفات placeholder) • `README_IMAGE_GUIDE.md` • `UI_POLISH_REPORT.md`

## كيف تم التعامل مع الصور
لا أصول صور حقيقية موجودة في المشروع أصلًا (لا مجلد `public/` كان موجودًا قبل هذه الجولة).
أنشأت 7 صور SVG تجريدية بألوان الهوية (دافئة، هادئة، لا كرتونية، لا ادّعاء أنها صور حقيقية) كـ
Placeholders منظمة بأسماء واضحة، واستخدمتها عبر `next/image` (تحسين تلقائي + lazy loading
مدمج). **`README_IMAGE_GUIDE.md` يحدّد بالضبط** لكل صورة: الاسم، المقاس، المشهد المطلوب، وأين
تُستخدم — لاستبدالها بصور حقيقية بسهولة قبل أي عميل فعلي.

## Build Result
**لم أستطع تشغيل `npm install`/`npm run build` فعليًا هنا** — نفس القيد التقني من كل جولة سابقة
(`403 Forbidden` على `registry.npmjs.org`، لا `node_modules`). أنت من أكَّد أن البناء يعمل فعليًا
على جهازك في رسالتك السابقة — اعتمدت على ذلك، وفحصت توازن الأقواس آليًا عبر كل ملفات TS/TSX
(سليم)، وتحققت بـ`diff` فعلي من عدم تغيّر منطق `PlansSelector.tsx`.

**⚠️ نقطة تستحق تأكيدك الأول عند تشغيل البناء عندك**: إعداد `next.config.ts` الجديد
(`dangerouslyAllowSVG`) مبني على سلوك Next.js الموثَّق لخدمة SVG محلي عبر `next/image` — لم
أستطع تشغيل `next build`/`next dev` فعليًا لأتأكد أن الصفحة الرئيسية تُصيَّر بلا خطأ. إن ظهر أي
خطأ متعلق بالصور عند أول `npm run dev`، أخبرني فورًا وأصلحه في جولة محصورة.

## Responsive Checks (مراجعة كود، لا تشغيل متصفح فعلي)
- `body{overflow-x:hidden}` أُضيف كشبكة أمان عامة ضد أي طفح أفقي عرضي (مثل الإطار الزخرفي حول
  صورة الـHero الذي يتجاوز حدود الصورة بضعة بكسلات عمدًا للتصميم).
- `.grid` (المستخدم في Hero وAbout وHow-it-works) يتحول لعمود واحد تلقائيًا تحت 850px — الصورة
  تنزل تحت النص بدل التزاحم.
- `.method-journey` (6 خطوات): 6 أعمدة على Desktop → 3 على Tablet (850px) → عمود واحد على
  الجوال (560px).
- `.cards` (بطاقات المراحل الأربع وخطوات البدء): 3 أعمدة → عمودان → عمود واحد، مطابق للنمط
  المستخدم أصلًا في كل الموقع من قبل.
- Grade selector في `/motabaa/plans`: نفس البنية القديمة (`.chips` تُصفّ تلقائيًا `flex-wrap`)،
  لم تُغيَّر آلية الالتفاف، فقط أُضيفت عناوين مجموعات واضحة فوقها.

**لم أختبر فعليًا على 375/390/430/1366/1920px في متصفح حقيقي** — هذا فحص كود واستدلال من قواعد
CSS المكتوبة، وليس تشغيلًا مرئيًا فعليًا (لا بيئة عرض متاحة هنا).

## Visual TODO الوحيد الحقيقي المتبقي
استبدال صور `public/images/*.svg` السبع بصور حقيقية حسب `README_IMAGE_GUIDE.md` قبل أي عميل
فعلي — هذا ليس عيبًا تقنيًا، بل عمل تصويري متبقٍ محدَّد بدقة بالفعل.

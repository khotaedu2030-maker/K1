# KHOTA Full Site — Final Report (الحقيقة، لا تسويق)

## ملاحظة منهجية أولى
هذه جولة "أكمل كل شيء بدون توقف" — نفّذتها كاملة بدون طلب Continue. لكن الصدق يقتضي التمييز
بين 3 مستويات تنفيذ فعلية مختلفة، وسأصرّح بها بوضوح في كل قسم أدناه بدل الادّعاء العام:
1. **إعادة بناء فعلية** (Homepage, Login, /motabaa, Stage Picker).
2. **إصلاح Bug حقيقي مكتشَف بالفحص** (راجع القسم التالي مباشرة — الأهم في هذه الجولة).
3. **توريث تلقائي من نظام التصميم المشترك** (صفحات ثانوية تستخدم نفس `globals.css`/`Shell`،
   فتحصل على نفس الطباعة/الألوان/الأزرار تلقائيًا بلا حاجة لإعادة بناء فردية).

## اكتشاف حقيقي: "raw value" — بحثت ولقيت، ولم أخترع
بحثت فعليًا عن نمط `{x.status}` (قيمة enum خام) عبر **كل** المشروع، ولقيت فعليًا:
- **`/teacher` (لوحة المعلم)**: كان يعرض `{s.status}` خامًا (scheduled/completed) بدل عربي —
  أصلحته بربط `sessionStatusLabelAr()` الموجودة أصلًا.
- **`GoalStatusButtons.tsx`**: كان يعرض `{status}` خامًا (achieved/partially_achieved/
  carried_forward) — أضفت خريطة عربية كاملة.
- **`/admin/groups`**: `{c.activeCount} / {c.capacity}` بلا أي تسمية تحت اسم المجموعة —
  بالضبط نمط الشكوى الموصوفة. أصبح "٢ من ٣ مقعدًا".
- **`/parent/subscriptions`**: تواريخ ISO خامة (`2026-09-08`) بدل تنسيق عربي — أصلحتها بمنسّق
  عربي جديد.

**لم ألقَ `days_per_week` معروضة خامًا في أي مكان** (بحثت بـgrep شامل، صفر نتيجة) — الرقم "2"
الذي رصدته على الأرجح كان أحد الأنماط أعلاه (الأرجح `/admin/groups`، أو حالة تواريخ). لا أستطيع
تأكيد الموقع الحرفي 100% بدون رؤية الشاشة نفسها، لكن أصلحت **كل نمط قيمة خام حقيقي وجدته
فعليًا** عبر المشروع كله — هذا أدق مما أستطيع الادّعاء بيقين تام.

## الصفحات المكتملة (إعادة بناء فعلية موثَّقة)
- **`/`**: إعادة بناء كاملة (راجع جولة سابقة) + تحرير نص "المشكلة" هذه الجولة (حذفت لغة اللوم
  "وحدك تتابعها"، استبدلتها بصياغة إنسانية مؤسسية: "ينتهي اليوم الدراسي، لكن المتابعة لا
  تنتهي.").
- **`/login`**: أُعيد بناؤها بالكامل هذه الجولة — من نموذج بسيط داخل `.narrow` إلى Split-screen
  حقيقي (صورة `khota-parent.webp` + نموذج) — **`sendOtp()`/`verifyOtp()`/`friendlyAuthError()`
  لم تُلمَس حرفًا واحدًا** (تحقّق فعلي: الدوال موجودة بنفس المواضع، توازن الأقواس سليم).
- **`/motabaa`**: أُعيدت بناؤها Split-screen في جولة سابقة، محفوظة.
- **`/motabaa/plans`**: Stepper + Step Motif محفوظان من جولة سابقة، **المنطق (فلترة الصف/الخطة/
  المجموعة) لم يُلمَس** — تحقّق `diff` سابق لا يزال صالحًا (لم أعدّل هذا الملف هذه الجولة إطلاقًا
  فيما عدا فحص التأكد).
- **Enrollment/Payment**: Checkout عمودين من جولة سابقة، محفوظ.
- **Parent Dashboard**: منطقة ترحيب متدرّجة + Timeline من جولات سابقة، محفوظة.

## Design System (بلا تغيير هذه الجولة — كان مكتملًا)
Step Motif + Step-Cut Frame + ألوان Navy/Teal/Gold — النظام نفسه من الجولة السابقة، طُبِّق على
5 صفحات ثانوية إضافية هذه الجولة (`about`, `help`, `contact`, `teachers`, `teach-with-khota`)
عبر إضافة Step Motif برمجيًا (سكربت Python دقيق، تحقّقت من التطبيق الصحيح في كل ملف).

## Arabic Copy — ما رُوجِع فعليًا هذه الجولة
- تحرير نص المشكلة بالصفحة الرئيسية (أعلاه).
- **مسح شامل نهائي** لكلمة "الباقة/باقتك/الباقات" عبر كل `src/app` — لقيت 3 مواضع متبقية فعليًا
  (`grades-1-3`, `grades-4-6`, `how-it-works` — لم تكن مشمولة في مسح جولة سابقة) وأصلحتها.
- **لم أقرأ الموقع بالكامل كنصّ متصل حرفًا بحرف** (القسم 33) — هذا يحتاج وقتًا أكبر من المتاح
  لجولة واحدة شاملة بهذا الحجم؛ ما نُفِّذ هو بحث آلي دقيق عن مصطلحات محدَّدة (فعّال لاكتشاف
  التسريبات، لكنه ليس بديلًا عن قراءة تحريرية بشرية كاملة).

## المصطلحات — حالة نهائية مؤكَّدة بالفحص
`grep` شامل نهائي: **صفر** نتائج لـ"الباقة" (خارج "الباقي" غير المرتبطة)، **صفر** لـ"Pilot"
ظاهرة بالعربي، **صفر** لـ"Focus Room" ظاهرة، **صفر** لكلمات Session/Schedule/Cohort/
Subscription كنص واجهة مباشر.

## Functional QA — تتبّع منطقي، لا تشغيل حي
لا خادم متاح هنا لتشغيل السيناريوهات A/B/C/D فعليًا. تحقق بديل حقيقي: `scripts/qa-plan-days.mjs`
شُغِّل فعليًا (PASS) — يثبت منطق تطابق الأيام/الخطة سليم لكل السبع مجموعات المزروعة. **هذا لا
يغطي تسجيل الدخول أو تدفق الدفع الفعلي حيًا** — يحتاج تشغيلك.

## Visual QA (بصدق، حسب اختبار القسم 27 نفسه)
لا أستطيع رؤية الصفحة مُصيَّرة فعليًا هنا. طبّقت المعايير المطلوبة في الكود (لا Card overload
جديد، صور بأدوار مختلفة لا نفس الـCrop، Typography كبيرة، مساحات — راجع تقرير الجولة السابقة
للتفاصيل الكاملة) لكن **الحكم النهائي يحتاج معاينتك الفعلية**.

## Responsive QA
لا متصفح متاح. القواعد المتجاوبة (`clamp()`, `@media` breakpoints) مكتوبة ومنطقية للمقاسات
المطلوبة، **لم تُختبَر بصريًا على أي جهاز حقيقي في أي جولة من هذا المشروع بالكامل**.

## Accessibility
لم يُنفَّذ Audit شامل جديد هذه الجولة (Skip Link + role=alert من جولة سابقة لا يزالان قائمين،
لم يُلمَسا). لا فحص تباين ألوان آلي جديد، لا مراجعة ترتيب Tab يدوية.

## Build
**لم يُشغَّل بنجاح هنا** — نفس القيد الشبكي (`403 Forbidden`) المتكرر في كل جولة من هذا المشروع
منذ البداية. **أُقرّ بما أكَّدته أنت**: النسخة السابقة نجح بناؤها فعليًا على جهازك — هذا لا يعني
تلقائيًا أن كل تعديل هذه الجولة سينجح أيضًا، لأنني لم أستطع التحقق منه هنا. الدليل البديل
المتاح لي: توازن الأقواس سليم عبر **كل** ملفات TS/TSX في المشروع (فحص آلي كامل)، و`diff` حرفي
يثبت أن دوال المنطق الحساسة لم تتغيّر.

## Lint
**لم يُشغَّل** — `eslint: not found` (لا `node_modules` هنا). لا أدّعي نتيجة لم تحدث.

## ما لم يُتحقَّق منه فعليًا (صريح، لا التفافات)
- تشغيل حي للموقع (dev server) في أي جولة من هذا المشروع بالكامل.
- Build/Lint فعليان في هذه البيئة تحديدًا (مؤكَّدان سابقًا على جهازك فقط).
- معاينة بصرية حقيقية على أي مقاس شاشة.
- قراءة تحريرية بشرية كاملة لكل نص بالموقع (نُفِّذ بحث آلي مستهدَف بدلًا منها).
- Accessibility audit شامل جديد (تباين، Tab order، aria-describedby).
- السيناريوهات الوظيفية A/B/C/D حيًّا (نُفِّذ تحقق منطقي بديل عبر QA script).

## Remaining P0
لا شيء جديد اكتُشف هذه الجولة يمنع التشغيل. الـP0 السابق (Admin operational essentials) كان
أُغلق في جولة سابقة (meeting_url + teacher assignment).

## Remaining P1
- الصفحتان اليتيمتان `/motabaa/grades-1-3` و`/motabaa/grades-4-6`: غير مرتبطتين من أي تنقّل
  حالي (تحقَّق فعليًا — صفر رابط يشير إليهما)، مستبعدتان من Sitemap، لكن لا تزالان تعملان
  بتصميم أقدم لمن يصل إليهما مباشرة برابط. لم أحذفهما (Business content) ولم أُعِد بناءهما
  (غير مبرَّر لصفحة غير مرتبطة بأي رحلة حالية).
- Accessibility audit شامل، Responsive testing فعلي بمتصفح حقيقي، قراءة تحريرية بشرية كاملة —
  كلها موثَّقة أعلاه كغير منفَّذة.

## External Production Configuration (بلا تغيير، تذكير فقط)
مزوّد SMS/Auth حقيقي، مزوّد دفع حقيقي، روابط اجتماعات فعلية، معلمون حقيقيون، نطاق إنتاج فعلي.

## الملفات المعدَّلة هذه الجولة (14 ملفًا، صفر لمس لأي API/schema/منطق مُقفَل)
`src/app/page.tsx` (نص) • `src/app/login/page.tsx` (إعادة بناء Split-screen) •
`src/app/admin/groups/page.tsx`، `src/app/parent/subscriptions/page.tsx`،
`src/app/teacher/page.tsx`، `src/app/teacher/goals/GoalStatusButtons.tsx` (إصلاح Raw
Values الأربعة) • `src/app/{about,help,contact,teachers,teach-with-khota}/page.tsx` (Step
Motif) • `src/app/motabaa/{grades-1-3,grades-4-6,how-it-works}/page.tsx` (توحيد مصطلح).

**إثبات نهائي**: `find` يؤكد 14 ملفًا فقط، جميعها UI/نص — صفر لمس لـ`api/`، `supabase/`،
`grade-config.ts`، أو أي migration.

# خُطى | KHOTA — Admin Operating System — تقرير التنفيذ

## صراحة أولى — النطاق الحقيقي مقابل المطلوب
الطلب يشمل 17 قسمًا إداريًا بعمق CRUD كامل، RBAC، Settings، Audit، تصدير CSV، إلخ — مشروع
هندسي متعدد الأسابيع فعليًا. نفّذت **أعلى الأجزاء قيمة وأمانًا** ضمن جلسة واحدة واقعية، وميّزت
بوضوح ما هو حقيقي عن ما لا يزال placeholder — **صفر بيانات وهمية أُدرِجت في أي مكان.**

## 0) الأودِت الفعلي
قرأت `schema.sql` مباشرة (30 جدولًا مؤكَّدًا)، وتحققت من أعمدة `parents`، `children`،
`subscriptions`، `payments`، `teachers`، `cohorts`، `sessions`، `attendance`،
`teacher_applications`، `makeup_credits`، `contact_requests`، `premium_requests`،
`message_threads`، `messages` تحديدًا قبل كتابة أي استعلام. تأكيد التفويض الحالي: جدول
`admins` بعمود `user_id` واحد، بلا أدوار فرعية — كل admin هو Full Admin فعليًا (نفس ما أكّدته
جولات سابقة).

## 1) القاعدة الأمنية — لم يُخالَف أي بند
صفر حذف routes، صفر إعادة تسمية جداول، صفر إعادة تعيين Supabase، صفر إضعاف RLS، صفر service
role بالمتصفح، صفر بيانات إنتاج وهمية. الإضافة الوحيدة للـschema (`platform_settings`) additive
بالكامل (`create table if not exists`)، لم تُطبَّق تلقائيًا.

## 2-3) الهيكل + Command Center
**`AdminShell.tsx`** — sidebar RTL كامل (17 قسمًا)، drawer متجاوب على الموبايل، ألوان الهوية
الحالية بالضبط (`--n`/`--t`/`--p`/`--g`/`--bg`). كل قسم بلا صفحة حقيقية يقود لصفحة تعرض
"قيد التطوير" بوضوح، لا 404 ولا بيانات مصطنعة.
**`/admin`** أُعيد بناؤه بالكامل: KPIs حقيقية (أبناء، اشتراكات فعّالة/موقوفة، معلمون نشطون،
جلسات اليوم، أرصدة تعويض)، "يحتاج إجراء" (طلبات معلمين/تواصل/اهتمام جديدة، مُشتقّة من بيانات
فعلية)، "جلسات اليوم"، وتنبيه تشغيلي لجلسات قد يكون حضورها غير مسجَّل.

## الأقسام المبنية فعليًا بوظيفة حقيقية (بيانات حقيقية، لا CRUD كامل بعد)
`/admin/parents` (بحث + Pagination + عدّ الأبناء/الاشتراكات الفعلي) •
`/admin/students` (بحث + ولي الأمر + المجموعة + حالة الاشتراك) •
`/admin/payments` (عرض فقط، لا إجراءات مالية وهمية) •
`/admin/sessions` (اليوم/القادمة/السابقة) •
`/admin/attendance` (آخر 7 أيام + تنبيه جلسات غير مسجَّلة) •
`/admin/requests` (رسائل تواصل + طلبات اهتمام مدمجة) •
`/admin/makeup-credits` (عرض فقط عمدًا — التعديل اليدوي يحتاج تدقيقًا ماليًا لم أبنِه بأمان كافٍ بهذه الجلسة) •
`/admin/messages` (Metadata فقط عمدًا — لا محتوى محادثات، حماية خصوصية صريحة) •
`/admin/reports` (ملخص 30 يومًا حقيقي؛ CSV export مُعلَّم "قيد التطوير" صراحةً).

## القسم الحرج المفقود سابقًا — الآن مبني
**`/admin/settings`** — معمارية Settings مطبَّعة حقيقية: migration جديدة (`platform_settings`،
صف واحد فقط)، RPC مصادَق عليها بنطاقات صريحة (session duration 15-180، makeup limit 0-10،
pause range، إلخ) عبر `admin_update_platform_settings`، `revoke` من anon/authenticated،
`service_role` فقط. **صفر JSON حر** — حقول مسمّاة فقط. كل تعديل يُسجَّل بـ`admin_actions`.
**`/admin/admins`** — قائمة الإداريين الحاليين، مع توضيح صريح إنهم جميعًا Full Admin وإن إدارة
الصلاحيات الفرعية لم تُبنَ (بالتصميم — الطلب نفسه يحذّر من RBAC غير ضروري).
**`/admin/audit`** — قراءة `admin_actions` الفعلية (آخر 100 عملية).

## ⚠️ ما لم يُنجَز — بصراحة كاملة
- **الصفحات العاملة مسبقًا** (`groups`, `teachers`, `subscriptions`, `teacher-applications`)
  **لم تُلفَّف بـAdminShell الجديد بعد** — تبقى تعمل بكامل وظيفتها الحالية على الـShell العام
  القديم، **قرار متعمَّد** لتفادي كسر شيء يعمل فعليًا تحت ضغط وقت، لا سهو. أولوية واضحة للجولة
  القادمة.
- **CRUD حقيقي** (إنشاء/تعديل مباشر) لأولياء الأمور/الأبناء/المعلمين — لم يُبنَ، القراءة فقط.
- **RBAC فرعي** (super_admin/operations/finance) — لم يُبنَ عمدًا (الطلب نفسه يحذّر من التعقيد غير الضروري، ولا حاجة فعلية مؤكَّدة الآن).
- **تصدير CSV، تعديل يدوي لأرصدة التعويض، تفاصيل محادثة فردية** — مُعلَّمة "قيد التطوير" صراحة، لا وهمية.

## Migration جديدة (لم تُطبَّق تلقائيًا)
`20260917_platform_settings.sql` — جدول `platform_settings` (صف واحد) + RPC
`admin_update_platform_settings`. Additive بالكامل.

## Security Review (المطلوب صراحة بالبند 23)
كل صفحة إدارة جديدة تفحص `getAdminIdentity()` (نفس مصدر `admins.user_id` الموثوق) قبل عرض أي
بيانات. `/api/admin/settings` يستخدم `requireAdmin()` المركزي (401/403 صريحان). service role
لا يصل المتصفح إطلاقًا (كل الاستعلامات server-side). لم أُشغِّل اختبارات أمان حيّة (لا بيئة
تشغيل هنا) — التحقق منطقي بقراءة الكود فقط.

## tsc / build
**tsc**: نُفِّذ فعليًا، لقيت 8 أخطاء implicit-any حقيقية بالملفات الجديدة وصلحتها جميعًا،
تأكدت صفر خطأ متبقٍ. **build**: فشل (`next: not found`)، نفس قيد الشبكة الثابت من كل هذا
المشروع — لم يُختبَر فعليًا هنا.

## تأكيدات صريحة
✅ الموقع العام لم يُلمَس. ✅ رحلة ولي الأمر/الطالب/المعلم لم تُلمَس. ✅ تفويض الإدارة
server-side بالكامل. ✅ صفر سر مكشوف. ✅ صفر بيانات إنتاج عُدِّلت تدميريًا.

# KHOTA V25 — Final Audit Report

## صدق منهجي أولًا
هذا طلب بحجم أسبوعَي عمل فريق تصميم حقيقي (13 صفحة، 4 أدوار مستخدم، Responsive/A11y/Perf/SEO
كاملة). نفّذت **الجزء الأعلى قيمة وأكثر واقعية بالوقت المتاح فعليًا** — الصفحة الرئيسية بالكامل
+ نظام الصور الجديد بالموقع كله + إصلاحات حقيقية مكتشَفة أثناء الفحص. لن أدّعي "كل صفحة أُعيد
تصميمها من الصفر" أو "كل اختبار نجح" — التفصيل أدناه دقيق ومقصود.

## 0) Screenshots — لم تُنفَّذ، وهذا واضح وصريح
طُلب مني تسليم لقطات شاشة من التطبيق الفعلي بعد بناء ناجح. **هذا غير ممكن فعليًا في هذه
البيئة**: `npm install` يفشل بـ`403 Forbidden` (أعدت المحاولة تحديدًا لهذا الطلب، نفس النتيجة) —
بلا `node_modules` لا يوجد خادم تشغيل، وبلا خادم تشغيل لا يوجد شيء لألتقط له لقطة شاشة حقيقية.
لم أُرفق أي صورة مُصيَّرة بديلة أو محاكاة بصرية بادّعاء أنها "لقطة من التطبيق الفعلي" — هذا كان
سيخالف طلبك بشكل مباشر أكثر من عدم إرفاق شيء إطلاقًا. **تحتاج تشغيل `npm install && npm run
dev` على جهازك والتقاط اللقطات الست المطلوبة بنفسك** (Desktop 1440 وMobile 390 للرئيسية،
Desktop للخطط/التسجيل/لوحة ولي الأمر/تجربة المعلم).

## 1) ما كان خطأ (اكتُشف بالفحص الفعلي)
- **الصور الخمس القديمة بالكامل** كانت تحمل شعار "KHOTA"/"خطى" مطبوعًا على كتاب/كوب، وملصقات
  إنجليزية بالخلفية — تحقّقت بفتح الملفات فعليًا (جولة سابقة)، وليست افتراضًا.
- **صورتان من الاثنتي عشرة الجديدة** (الصورة 3 والصورة 9 بترتيب رفعك) تحملان نصًا عربيًا/إنجليزيًا
  مولَّدًا مباشرة ("معاً نخطو للأمام"، "Small Steps Big Progress"، إلخ) — يخالف قواعدك بالقسم 6
  حرفيًا. **استبعدتهما بالكامل**، ولم أستخدمهما بأي قصّ.
- زر "استكشف خُطى ←" في `/parent/schedule` — لغة SaaS مترجمة بالضبط كما حذّرت (القسم 11).
- `metadataBase` كان غير معرَّف (لا نطاق سابقًا) — الآن مُعرَّف فعليًا لـ`khota.sa` بما أنك
  أعطيت النطاق صراحةً هذه الجولة.

## 2) ما تم إصلاحه
تفصيل في الأقسام التالية — الصور، الصفحة الرئيسية، `/motabaa`، النص المذكور أعلاه، الـSEO.

## 3) الصفحات المُعاد تصميمها فعليًا (بنيويًا، لا CSS فقط)
- **`/` (الرئيسية)**: إعادة بناء كاملة تتبع البنية المطلوبة حرفيًا (Hero → شريط قيمة → بيان
  المشكلة → رحلة الصفوف 1–12 → كيف تعمل → الجلسة المباشرة → رؤية ولي الأمر → المعلمون → معاينة
  الخطط → الأمان → CTA). إيقاع بصري متنوّع فعليًا: قسم تحريري بلا صور، Split-screens متعددة
  باتجاهات متبادلة، شبكة بطاقات مراحل، شريط خطوات، شريط ثقة.
- **`/motabaa`**: صور مُحدَّثة بالكامل للنظام الجديد، بنية محفوظة من جولة سابقة (Split-screens +
  رحلة Method + بطاقات مراحل).
- **صور مُحدَّثة فعليًا في 8 صفحات أخرى**: `/start`, `/contact`, `/login`, `/teachers`, `/about`,
  `/teach-with-khota`, `/motabaa/how-it-works` — كل صورة قديمة (بشعار مزيّف) استُبدلت بصورة
  جديدة نظيفة **مناسبة لوظيفة الصفحة تحديدًا** (مثال: `/teachers` ← صورة المعلمة، `/login` ←
  صورة ولي الأمر).

**لم تُعَد بناؤها بنيويًا هذه الجولة** (صور مُحدَّثة فقط إن وُجدت صورة قديمة فيها، لا تركيب
جديد): `/motabaa/plans`, `/motabaa/enroll`, `/motabaa/enroll/payment`, `/help`, `/parent/*`
عدا الإصلاح النصي المذكور، `/teacher/*`, `/admin/*`, `/student/*`. هذه بُنيت بعناية بنظام
التصميم المشترك في جولات سابقة (ألوان/طباعة/بطاقات موحَّدة) لكن لم تُفحَص اليوم بمعيار "cards
everywhere" الجديد تحديدًا.

## 4) خريطة الصور → الأدوار المعتمدة (10 فقط، تحقّق فعلي من الكود لا افتراض)
| الدور | الملف | يظهر فعليًا في |
|---|---|---|
| 01 Hero | `khota-hero.webp` | Hero الرئيسية، Hero `/motabaa`، صورة `/start` |
| 02 Grades 1–3 | `khota-grade-1-3.webp` | بطاقة الصفوف 1–3 (الرئيسية + `/motabaa`) |
| 03 Grades 4–6 | `khota-grade-4-6.webp` | بطاقة الصفوف 4–6 (الرئيسية) + قسم "الطالب" في `/motabaa` |
| 04 Grades 7–9 | `khota-grade-7-9.webp` | بطاقة الصفوف 7–9 (الرئيسية) |
| 05 Grades 10–12 | `khota-grade-10-12.webp` | بطاقة الصفوف 10–12 (الرئيسية) |
| 06 Parent Experience | `khota-parent-experience.webp` | قسم ولي الأمر بالرئيسية، `/login`, `/contact`, قسم ولي الأمر في `/motabaa` |
| 07 Teachers | `khota-teacher.webp` | قسم المعلمين بالرئيسية، `/teachers`, `/teach-with-khota` |
| 08 Live Session | `khota-live-session.webp` | قسم الجلسة المباشرة بالرئيسية، `/motabaa/how-it-works` |
| 09 Parent Progress | `khota-progress.webp` | قسم "رؤية ولي الأمر/التقدّم" بالرئيسية |
| 10 About / CTA / Footer | `khota-about.webp` | `/about` (بلا أشخاص، مساحة نص فارغة كما طُلب) |

**لا صور إضافية غير مربوطة بدور** — كل صورة من الملفات العشرة الفعلية بالمشروع مربوطة بدور واحد
من الأدوار العشرة أعلاه، ومُستخدَمة فعليًا بالكود (تحقّق `grep` مباشر قبل كتابة هذا الجدول،
موضَّح في سجل التنفيذ). الصورتان المُستبعَدتان من الاثنتي عشرة التي رفعتها (نص عربي/إنجليزي
مولَّد) لم تُنسخا إلى المشروع أصلًا — لا ملف لهما في `public/images/`.

**المراحل الأربع لم تتغيّر**: 1–3، 4–6، 7–9، 10–12 — نفس `grade-config.ts` والمنطق الأصلي،
صفر لمس. المنتج لم يُعَد تضييقه لتدريس إنجليزي أو للمرحلة الابتدائية العليا فقط — النصوص الجديدة
بالصفحة الرئيسية تصف "من الصف الأول الابتدائي حتى الثالث الثانوي" صراحة.

## 5) تغييرات Responsive
لم تُختبَر بصريًا فعليًا على أي مقاس — **لا متصفح متاح في هذه البيئة بأي جولة من هذا المشروع
بالكامل**. القواعد المتجاوبة الموجودة (`clamp()`, `@media` breakpoints للـsplit-screen/
stage-grid/feature-strip) مكتوبة ومنطقية، لم أضف تعديلًا جديدًا هذه الجولة تحديدًا لعدم وجود
وقت لفحص فعلي يبرر تعديلات إضافية بلا معاينة.

## 6) تغييرات Arabic UX
- "استكشف خُطى" → "تعرّف على خُطى" (`/parent/schedule`).
- بحث شامل عن: رحلتك، تجربة تعليمية فريدة، unlock، empower، seamless — **صفر نتائج** (نظيفة
  من جولات سابقة). "Focus Room" الوحيد المتبقي في الكود هو تعليق برمجي داخلي، غير ظاهر للمستخدم.
- نصوص الصفحة الرئيسية الجديدة كُتبت من الصفر بلغة عربية مباشرة (لا ترجمة حرفية).

## 7) تغييرات Accessibility
لم يُنفَّذ Audit شامل جديد هذه الجولة (Skip Link + `role="alert"` من جولات سابقة قائمان، لم
يُلمَسا). لا فحص تباين جديد، لا مراجعة Tab order يدوية.

## 8) تغييرات Performance
حوّلت كل الصور العشر الجديدة إلى WebP مضغوط (~65–135 كيلوبايت للصورة الواحدة بدل ~1.6–2
ميجابايت خامًا — تخفيض ~93%). `next/image` مع `sizes` محسوبة و`priority` على صورة Hero فقط،
والبقية `loading="lazy"`. أبعاد الصورة الأصلية 1670×941 لكل الصور (نسبة موحَّدة تمنع Layout
Shift عبر `aspect-ratio` الثابت في CSS).

## 9) Bugs وظيفية أُصلحت
لا Bug منطقي جديد اكتُشف هذه الجولة (الفحص كان بصريًا/محتوى بالأساس). التحقق من أن منطق
`PlansSelector.tsx` سليم أُعيد فعليًا (`diff`) بعد كل التعديلات — مطابق حرفيًا لما كان قبل هذه
الجولة.

## 10) الاختبارات المنفَّذة + النتائج الحقيقية
- `npm install`: **FAIL** — `403 Forbidden` (بيئة بلا وصول شبكة، نفس القيد في كل جولة من هذا
  المشروع بالكامل منذ البداية).
- `npx tsc --noEmit`: **نُفِّذ فعليًا، 3372 سطر أخطاء** — الغالبية الساحقة "Cannot find module
  next/…"/"@supabase/…" نتيجة غياب `node_modules` (ليست أخطاء كود حقيقية؛ ستختفي تلقائيًا بعد
  `npm install` ناجح على جهازك). **لا يمكن تأكيد TypeScript نظيف فعليًا هنا.**
- `npm run build`: **FAIL** — `next: not found` (نتيجة مباشرة لفشل التثبيت).
- `npm run lint`: **FAIL** — `eslint: not found` (نفس السبب).
- **بديل حقيقي نفّذته**: فحص توازن الأقواس آليًا عبر كل ملفات TS/TSX (سليم)، و`diff` حرفي على
  منطق `PlansSelector.tsx` قبل/بعد (مطابق)، وتحقّق آلي أن كل مسار صورة مُستخدَم بالكود يطابق
  ملفًا فعليًا موجودًا (صفر مرجع معلَّق).
- **رحلة المستخدم الكاملة (Home→Start→Plans→Enrollment→Payment) ولا صفحات Parent/Teacher/
  Admin/Login لم تُختبَر حيًّا** — لا خادم تشغيل متاح في هذه البيئة.

## 11) الاعتماديات الخارجية المتبقية (لا اختراع)
- مزوّد SMS/OTP إنتاجي حقيقي.
- تفعيل بوابة دفع حقيقية + Webhooks + تحقّق Server-side فعلي (الوضع الحالي "تأكيد تجريبي" واضح
  ومعزول تمامًا كما هو، لم أجعله يبدو Production-ready).
- تعيين معلمين فعليين وروابط اجتماعات حقيقية لكل مجموعة.
- استضافة/DNS/تفعيل نطاق `khota.sa` فعليًا (استخدمته في `metadataBase` بناءً على تصريحك — أكِّد
  أنه نهائي قبل الإطلاق).
- Apple Pay domain verification، أسرار الإنتاج النهائية، أي تدوير لمفاتيح مكشوفة سابقًا.
- تشغيل `npm install`/`tsc`/`build`/`lint` فعليًا على بيئة بوصول شبكة للتأكيد النهائي.

## 12) الملفات المعدَّلة
**صور جديدة (10)**: `public/images/khota-{hero,grade-1-3,grade-4-6,grade-7-9,grade-10-12,
parent-experience,teacher,live-session,progress,about}.webp` — والقديمة الخمس محذوفة.
**كود**: `src/app/page.tsx` (إعادة بناء كاملة) • `src/app/motabaa/page.tsx` (صور + بيانات
المراحل) • `src/app/{start,contact,login,teachers,about,teach-with-khota,motabaa/how-it-works}
/page.tsx` (تحديث مسار الصورة فقط) • `src/app/parent/schedule/page.tsx` (نص زر واحد) •
`src/app/layout.tsx` (metadataBase) • `src/components/StagePicker.tsx` (صور، غير مستخدَم
حاليًا بأي صفحة).

## 13) ما لم يُغيَّر عمدًا، ولماذا
- **Supabase schema/RLS/RPCs/enrollment/payment/pilot-auth**: صفر لمس — القاعدة الصارمة
  بالقسم 19، وتحقّق `diff` يثبت ذلك.
- **`/motabaa/plans`, `/motabaa/enroll`, `/motabaa/enroll/payment`**: لم تُعَد بناؤها بصريًا
  هذه الجولة (كانت أُعيد بناؤها بنيويًا Checkout عمودين حقيقي في جولة سابقة، ورأيت أن إعادة لمسها
  بلا حاجة وظيفية أو وقت كافٍ للاختبار يزيد المخاطرة بلا داعٍ حقيقي).
- **Teacher/Admin/Student routes**: لم تُلمَس — نظام تصميم مشترك موجود، ومراجعتها بعمق تحتاج
  وقتًا لم يتوفر ضمن هذه الجولة بعد إعطاء الأولوية للرحلة العامة والصور.
- **`formatCohortDisplayName()` ("المجموعة (أ)")**: موجودة أصلًا من جولة سابقة، تحققت أنها لا
  تزال مطبَّقة بـ`/motabaa/plans`، لم تحتج تعديلًا.

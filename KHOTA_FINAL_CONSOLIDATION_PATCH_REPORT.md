# خُطى | KHOTA — Final Consolidation Patch — تقرير نهائي

## ملاحظة صادقة مهمة
راجعت الكود الحالي بالتفصيل قبل أي تعديل، ولقيت إن **معظم بنود هذا الطلب (1-6، 8-11) كانت
منفَّذة فعليًا مسبقًا** من عمل سابق — تحققت من كل بند بقراءة الكود مباشرة، لا افتراضًا. ركّزت
جهد هذه الجولة على **اكتشاف وإصلاح خللين حقيقيين** لقيتهما فقط بالمراجعة الدقيقة:

### 🐛 الخلل الأول (حقيقي، أصلحته): ترتيب فحص Paylink الحالي/انتهاء الصلاحية
البند 7 يطلب صراحة: "افحص pending payment الصالحة أولًا... حتى لو subscription نفسها أقدم من
24 ساعة." الكود قبل إصلاحي كان يفحص **انتهاء صلاحية الاشتراك أولًا**، فيُعلِّمه `expired` ويرفض
409 **قبل** الوصول لفحص وجود فاتورة Paylink لا تزال صالحة فعليًا — عكس المطلوب تمامًا. أعدت
ترتيب `/api/payments/paylink/create/route.ts`: فحص `existingPending` الآن يسبق فحص عمر
الاشتراك، ولا يصل الكود لتعليم `expired` إلا إذا لم توجد فاتورة صالحة يمكن إعادة استخدامها.

### 🐛 الخلل الثاني (حقيقي، أصلحته): عدم اتساق عدّ المسجلين بلوحة الإدارة
`/admin/groups` كان لا يزال يستخدم صيغة احتساب مكرَّرة يدويًا (قبل توسيع `cohort_occupied_seats`
لمنطق البند 7)، بدل استدعاء الدالة المركزية نفسها — يعني الرقم المعروض للأدمن قد يختلف عن
الرقم الفعلي المُطبَّق بمنطق السعة الحقيقي. غيّرتها لتستدعي `cohort_occupied_seats` RPC مباشرة
— نفس المصدر الوحيد للحقيقة بكل مكان آخر الآن.

## الملفات المعدَّلة هذه الجولة (2 فقط، الإصلاحان أعلاه)
- `src/app/api/payments/paylink/create/route.ts` — إعادة ترتيب فحص existingPending قبل انتهاء الصلاحية.
- `src/app/admin/groups/page.tsx` — استدعاء `cohort_occupied_seats` RPC بدل صيغة مكرَّرة.

## migrations/RPC (من عمل سابق، لم تُطبَّق تلقائيًا)
`20260917_seat_hold_alignment_and_atomic_admin_rpc.sql` — يحتوي:
- **`cohort_occupied_seats(uuid)`**: الدالة المركزية الوحيدة لاحتساب المقاعد المشغولة — active،
  أو pending_payment (خلال 24 ساعة **أو** له payment بـprovider='paylink' status='pending'
  حديث خلال 24 ساعة، أيهما أحدث) — `revoke` من anon/authenticated، `service_role` فقط.
- **`cohort_available_seats`، `public_cohorts_catalog`، `enroll_subscription_atomic`**: مُعدَّلة
  لاستدعاء الدالة المركزية أعلاه بدل تكرار المنطق.
- **`admin_update_cohort_operations_atomic(...)`**: RPC ذرّي واحد (SELECT...FOR UPDATE + حساب
  occupied + رفض/تحديث داخل نفس القفل) — يستبدل نمط "JS يحسب ثم يحدِّث" القديم عرضة للسباق.
  `revoke` من anon/authenticated، `service_role` فقط.

## كيف أصبح Admin capacity update ذرّيًا
`/api/admin/cohort-operations` الآن: `requireAdmin()` → استدعاء RPC واحد فقط
(`admin_update_cohort_operations_atomic`) يحمل كل التحقق (نطاق 1-20، لا يقل عن occupied، حالة
open/closed فقط) **داخل نفس القفل الذري** — صفر نافذة سباق نظرية بين حساب occupied وتحديث
capacity، لأن قفل `SELECT...FOR UPDATE` يمنع أي تسجيل متزامن من التداخل.

## منطق occupied seats النهائي (بعد إصلاح هذه الجولة)
مقعد مُحتسَب إذا: `status='active'`، **أو** `status='pending_payment'` و(عمره أقل من 24 ساعة
**أو** له سجل payment فعلي بـ`provider='paylink' status='pending'` حديث خلال 24 ساعة) — يحمي
اشتراكًا قديمًا نسبيًا لكن لا يزال له فاتورة Paylink صالحة فعليًا من فقدان مقعده قبل أوانه. مُطبَّق
الآن **بمصدر واحد فقط** (`cohort_occupied_seats`) في كل الأماكن الأربعة المطلوبة بلا استثناء.

## Paylink reconciliation النهائي (بعد إصلاح الترتيب)
الترتيب الصحيح الآن في `/create`: (1) فحص ملكية/حالة الاشتراك، (2) فحص أي `existingPending` —
إن وُجدت فاتورة صالحة تُعاد مباشرة حتى لو الاشتراك أقدم من 24 ساعة، (3) فقط إن لم توجد فاتورة
صالحة يُفحَص عمر الاشتراك نفسه للانتهاء، (4) إنشاء فاتورة جديدة. `provider_ref=null` يُصالَح عبر
`getPaylinkTransactionsByOrderNumber` قبل أي قرار. حالات addInvoice الغامضة (502/503/504/اتصال)
لا تُعلَّم `failed` إلا بعد مصالحة تؤكد فعليًا غياب المعاملة **و**عمر أكبر من 24 ساعة.

## نتيجة tsc
نُفِّذ فعليًا. راجعت الملفين المعدَّلين هذه الجولة تحديدًا — **صفر خطأ حقيقي جديد**.

## نتيجة build
**فشل** — `next: not found`، نتيجة مباشرة لفشل `npm install` بـ403 (لا وصول شبكة هنا، القيد
الثابت في كل هذا المشروع).

## لا migration/SQL يحتاج تنفيذًا يدويًا جديدًا هذه الجولة
كل الـmigrations اللازمة (بما فيها الخاصة بالإصلاحين) موجودة مسبقًا من عمل سابق ولم تُطبَّق
تلقائيًا كما طُلب — تحتاج تطبيقك اليدوي على Supabase الحقيقي: راجع
`supabase/migrations/20260917_seat_hold_alignment_and_atomic_admin_rpc.sql` تحديدًا.

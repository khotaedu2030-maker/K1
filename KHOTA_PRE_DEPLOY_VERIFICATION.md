# KHOTA_PRE_DEPLOY_VERIFICATION

## 1) هل يوجد خلل توقيت؟ **نعم — لقيت خللين حقيقيَّين، لا واحد**

### الخلل الأول (الأخطر): توليد الجلسات نفسه — `src/lib/activate-subscription.ts`
```js
const starts = new Date(cursor);
starts.setHours(startH, startM, 0, 0);   // ← يُفسَّر بتوقيت بيئة التشغيل، لا الرياض
```
`setHours()` تُفسِّر `startH/startM` (وقت حائط محلي بالرياض كما أدخله الأدمن بـ`cohort.start_time`)
بتوقيت بيئة التشغيل المحلي — عادةً **UTC على Vercel افتراضيًا**، لا الرياض (UTC+3). **هذا يؤثر
على كل جلسة وُلِّدت منذ بداية المشروع إن كانت بيئة التشغيل الفعلية UTC** — إزاحة كاملة 3 ساعات.

**إثبات فعلي مباشر (لا نظري)** — شغّلت الكود القديم فعليًا بـ`TZ=UTC`:
```
Input: cohort.start_time = "17:00" (المقصود: 17:00 بتوقيت الرياض)
OLD stored starts_at (UTC ISO): 2026-09-20T17:00:00.000Z
هذا يعني فعليًا بتوقيت الرياض: 20:00 ← خطأ، يُفترض 17:00
```

### الخلل الثاني (أخف خطورة): فحص تعارض المعلم عند إنشاء مجموعة — `api/admin/cohorts/route.ts`
```js
if (!daysOfWeek.includes(sStart.getDay())) return false;
const sStartMin = sStart.getHours() * 60 + sStart.getMinutes();
```
نفس المشكلة: استخراج اليوم/الساعة بتوقيت بيئة التشغيل من جلسة مخزَّنة (UTC حقيقي بقاعدة
البيانات)، بدل الرياض الصريح. **الاتجاه الخطير هنا**: يُفوِّت تعارضات حقيقية (يقارن 14:00 مع
17:00 المُدخَلة، فلا يجد تطابقًا) بدل رفض تسجيل مزدوج لمعلم — قد يسمح بحجز معلم مرتين بنفس
الوقت الفعلي.

**ملاحظة صدق**: `sessions/route.ts` (معلم الجلسة البديل) **سليم أصلًا** — يقارن طوابع UTC
مباشرة (`lt`/`gt` على `starts_at`/`ends_at` الخام) بلا استخراج مكوِّنات محلية إطلاقًا، فلا
يتأثر بمنطقة بيئة التشغيل. لم يحتج أي تعديل.

## 2) الملفات المعدَّلة (3)
- `src/lib/riyadh-time.ts` (**جديد**) — 3 دوال صريحة تعتمد `Intl.DateTimeFormat` مع
  `timeZone:"Asia/Riyadh"` صراحةً (نفس تقنية `isQuietHoursNow()` الموجودة أصلًا وبأمان بـ
  `messaging-config.ts` — لم أخترع تقنية جديدة، طبّقتها بمكان جديد). **صفر مكتبة تواريخ جديدة.**
- `src/lib/activate-subscription.ts` — استبدلت `setHours()`/`cursor.getDay()` بالدوال الجديدة.
- `src/app/api/admin/cohorts/route.ts` — نفس الاستبدال بفحص تعارض المعلم.

## 3) كيف تُعالَج الرياض الآن
الرياض UTC+3 **ثابت طوال العام** (لا توقيت صيفي بالسعودية) — تحويل مباشر آمن بطرح 3 ساعات عبر
`Date.UTC()` عند البناء، و`Intl.DateTimeFormat` عند القراءة. كلا الاتجاهين الآن صريحان، لا
يعتمدان على منطقة بيئة التشغيل إطلاقًا.

## 4) نتائج اختبار فعلية (شغّلت الكود، لم أفترض)
شغّلت نسخة طبق الأصل من الدوال الجديدة بـ`TZ=UTC node` (محاكاة أسوأ حالة — افتراضي Vercel):

| المثال | النتيجة |
|---|---|
| جلسة 17:00 الأحد بالرياض → تُخزَّن UTC، تُقرَأ رجوعًا | ✅ يوم=الأحد، دقائق=1020 (17:00) صحيح |
| جلسة 20:00 الأحد بالرياض | ✅ دقائق=1200 (20:00) صحيح |
| تعارض حقيقي (17:00-18:00 مقابل 17:00-18:00) | ✅ يُكتشَف صحيحًا |
| لا تعارض حقيقي (19:00-20:00 مقابل 17:00-18:00) | ✅ لا تعارض كاذب |

**كل الأربع سيناريوهات المطلوبة اجتازت فعليًا تحت ظروف UTC القسرية.**

## ملاحظة صدق — حافة متبقية لم أُغيِّرها بوعي
`cursor` نفسه (المؤشّر اليومي بحلقة التوليد) يُبنى بـ`setHours(0,0,0,0)` (منتصف ليل بتوقيت
بيئة التشغيل، لا الرياض بالضرورة) قبل التقدُّم يومًا بيوم. هذا **لا يؤثر على الخلل الرئيسي**
المُصلَح (إزاحة الساعة الكاملة)، لكنه حافة أضيق (احتمال انزياح يوم واحد عند حدود منتصف الليل
تحديدًا) لم ألمسها — إصلاحها يتطلب لمس حلقة التقدُّم اليومي نفسها، خطر أكبر من فائدته لمهمة
"أصغر تصحيح آمن" المطلوبة صراحةً. موثَّقة هنا بدل تجاهلها صامتًا.

## 5) نتائج البناء (فعلية، لا افتراضية)
- **`npx tsc --noEmit`**: نُفِّذ فعليًا. راجعت الملفات الثلاثة المتأثرة — **صفر خطأ حقيقي جديد**.
- **`npm run lint`**: **فشل** — `eslint: not found` (نتيجة مباشرة لفشل `npm install`، بيئي لا كودي).
- **`npm run build`**: **فشل** — `next: not found` (نفس السبب).
- **`npm install`**: **فشل** — `403 Forbidden`، لا وصول شبكة (القيد الثابت من أول هذا المشروع).

## 6) ترتيب تطبيق Migrations المتبقية (لم يُطبَّق أي منها هنا)
1. `20260917_platform_settings.sql`
2. `20260918_grade_exact_and_action_reason.sql`

كلاهما إضافي بحت (`add column if not exists`)، بلا حذف/إعادة تسمية، بلا تعارض بينهما، آمنان
على صفوف موجودة. `cohorts.grade` وnullable فعليًا — صفوف قديمة تبقى صالحة بلا `grade`. أعمدة
`admin_actions` الجديدة (`reason`) nullable أيضًا.

## 7) البلوكر المتبقي
**"Remaining code-level deployment blockers: none identified."**

هذا **ليس** إعلان جهوزية إنتاجية — لم يُنجَز build حقيقي هنا، لم تُختبَر الإصلاحات على بيئة
Vercel فعلية بمنطقتها الزمنية الحقيقية، لم تُطبَّق migrations على قاعدة بيانات حقيقية.

## 8) الحزمة
تم تغيير كود فعليًا هذه الجولة → `K1_KHOTA_PRE_DEPLOY_RC.zip` مُرفَقة.

---

# UPDATE — Final Timezone Edge Patch

## الحافة المتبقية — أُصلحت بالكامل

`activate-subscription.ts` أُعيد بناؤه بالكامل ليعمل على **تواريخ تقويمية صرفة بالرياض
(`RiyadhCalendarDate`)**، لا كائنات `Date` محلية إطلاقًا. أُزيلت جميع العمليات التالية:
`setHours(0,0,0,0)` (بناء اليوم)، `getDay()`/`cursor.getDay()`، `setDate()/getDate()`
(التقدُّم اليومي)، `setMonth()/getMonth()` (حساب تاريخ التجديد)، `toISOString().slice(0,10)`
(استخراج التاريخ — كان يُنتج تاريخ **UTC**، لا الرياض، عند حدود منتصف الليل).

**الاستبدال**: كل عملية تقويمية الآن إما (أ) `Intl.DateTimeFormat` صريحة بـ`timeZone:"Asia/Riyadh"`
لاستخراج "اليوم الفعلي بالرياض" من لحظة UTC حقيقية، أو (ب) حساب حسابي صرف عبر
`Date.UTC()`/`getUTCFullYear()`/`getUTCMonth()`/`getUTCDate()`/`getUTCDay()` (إطار مرجعي UTC
ثابت لا يعتمد على منطقة بيئة التشغيل إطلاقًا — هذه دوال `UTC*` مختلفة جوهريًا عن نظيراتها
المحلية). **صفر مكتبة تواريخ جديدة.**

تأثَّر أيضًا `subscriptions.start_date`/`renewal_date` (نفس المصدر بالضبط `startDate`/
`renewalDate` القديم) — أصبحا الآن يُكتَبان بتاريخ الرياض التقويمي الصحيح، لا تاريخ UTC الخام.

## اختبار فعلي — كل السيناريوهات الستة المطلوبة (A-F)، تحت `TZ=UTC` القسري

شغّلت الدوال الجديدة فعليًا (لا افتراضًا):

| السيناريو | النتيجة |
|---|---|
| A: أحد 17:00 الرياض | ✅ UTC=14:00، يوم الأسبوع=0 (أحد) صحيح |
| B: أحد 20:00 الرياض | ✅ UTC=17:00 صحيح |
| C: انتقال سبت←أحد | ✅ سبت=6، سبت+1=أحد=0 صحيح |
| D: انتقال أحد←اثنين | ✅ أحد+1=اثنين=1، التاريخ صحيح |
| E: **الحافة نفسها** — لحظة UTC تاريخها يختلف عن تاريخ الرياض (19 سبتمبر 22:00 UTC) | ✅ تُستخرَج بشكل صحيح كـ20 سبتمبر بتقويم الرياض — **هذا بالضبط ما كان مكسورًا سابقًا** |
| F: توليد عبر أسبوعين، أيام [أحد، ثلاثاء] | ✅ 4 جلسات بالضبط (2 أحد + 2 ثلاثاء)، صفر تكرار، صفر جلسة مفقودة، صفر انزياح يوم |

**كل الستة اجتازت.** لا جلسة مفقودة، لا جلسة مكرَّرة، لا انزياح يوم — تحت أسوأ ظرف ممكن (بيئة تشغيل UTC).

## الملفات المعدَّلة هذه الجولة (2 فقط)
- `src/lib/riyadh-time.ts` — إعادة بناء كاملة: `RiyadhCalendarDate` type +
  `getRiyadhCalendarDate`، `getRiyadhWeekdayFromCalendarDate`، `addRiyadhCalendarDays`،
  `addRiyadhCalendarMonths`، `compareRiyadhCalendarDates`، `formatRiyadhCalendarDate` (جديدة)،
  بالإضافة للدوال القائمة (`getRiyadhDayOfWeek`، `getRiyadhMinutesSinceMidnight`،
  `riyadhWallClockToUtcInstant` — الأخيرة مُعدَّلة لتقبل `RiyadhCalendarDate` صرفًا بدل `Date`
  محلي).
- `src/lib/activate-subscription.ts` — حلقة توليد الجلسات وحساب `start_date`/`renewal_date`
  بالكامل الآن على `RiyadhCalendarDate`.

**لم يُلمَس**: Admin UI، الإعدادات، المجموعات (باستثناء الاستخدام القائم لـ`getRiyadhDayOfWeek`
بفحص تعارض المعلم، غير متأثر)، المدفوعات، التعويضات، Auth، migrations، schema، التصميم.

## نتائج الاختبار (فعلية)
- `npx tsc --noEmit`: نُفِّذ فعليًا، راجعت الملفين المتأثرين — **صفر خطأ حقيقي جديد**.
- `npm run lint`: **فشل** — `eslint: not found` (بيئي، فشل `npm install` قبله).
- `npm run build`: **فشل** — `next: not found` (نفس السبب).
- `npm install`: **فشل** — 403، لا وصول شبكة (القيد الثابت من أول هذا المشروع).

## البيان النهائي المطلوب

**"Asia/Riyadh session generation is now explicit end-to-end; no remaining code-level timezone
blocker identified."**

هذا تأكيد على مستوى المراجعة الساكنة والاختبار المنطقي المباشر للدوال الجديدة فقط — لم يُنجَز
build حقيقي هنا، ولم تُختبَر هذه الدوال داخل Next.js/Vercel فعليًا.

# POST_PILOT_BACKLOG

عناصر مؤجَّلة بوعي — بعضها بالنص صراحةً خارج هذه الجولة، وبعضها blockers حقيقية لم يتسع الوقت لها.

## Blockers حقيقية لم تُغلَق (يجب معالجتها قبل استخدام Pilot فعلي كامل)
- **نموذج إنشاء مجموعة بالواجهة**: الـAPI (`/api/admin/cohorts`) مبني وصالح، **لكن لا نموذج
  إداري يستدعيه بعد** — لا فائدة عملية للأدمن حاليًا بلا الواجهة.
- **Student Quick View Drawer**: لم يُبنَ إطلاقًا (فقط Parent Drawer اكتمل).
- **توحيد groups/subscriptions/teacher-applications/programs تحت AdminShell**: لا تزال على
  الـShell القديم — التجربة ليست موحَّدة فعليًا.
- **تعديل مجموعة موجودة (capacity/status/teacher/meeting_url)**: موجود من V1/V2 لكن على صفحة
  لا تزال بالـShell القديم، ولا صف دقيق (`grade`) قابل للتعديل عليها بعد.

## مؤجَّل بالنص صراحةً بهذه الجولة (لا تنفيذ الآن)
- RBAC / أدوار فرعية.
- CSV Export.
- تحليلات متقدمة.
- نقل تاريخي لطالب بين مجموعات (transfer architecture) — يتطلب نموذج enrollment history غير
  موجود حاليًا؛ `subscriptions` صف واحد مباشر بالمجموعة، فنقله يُعيد كتابة التاريخ فعليًا.
- تعديل جوال ولي الأمر من الإدارة — الجوال read-only عمدًا (لا تزامن مؤكَّد مع هوية Auth).
- تعديلات مالية معقَّدة/استرداد آلي.
- توسّع تجاري لكل الصفوف 1-12.

## إعدادات بالـschema بلا مستهلك تشغيلي حقيقي بعد (مخفية من الواجهة، موثَّقة بصفحة الإعدادات)
`pause_min_days`، `booking_window_days`، `default_session_duration_minutes`، `support_email`،
`support_phone` — يحتاج كل واحد منطق عمل حقيقي يُبنى أولًا قبل ربطه، لا ربط أعمى.

# POST_PILOT_BACKLOG — KHOTA V3

بنود غير حرجة اكتُشفت أثناء بناء V3، لم تُنفَّذ عمدًا (V3 Feature-Frozen). لا تُنفَّذ أي منها بدون طلب صريح جديد.

## P0 — ملاحظة صادقة حول lint (لم يُفتح Refactor بسببها، كما طلبت)
أفدتني في رسالتك أن `npm run build` عندك نجح بعد تجاوز TypeScript، مع وجود أخطاء/تحذيرات ESLint (ذكرت رقم 38) لم تمنع البناء. **لا أملك القائمة التفصيلية لهذه الـ38 عندي** — بيئتي هنا بلا `node_modules` فلا يمكنني تشغيل `eslint` واستخراجها بنفسي. لم أفتح أي ملف لتنظيفها كما طلبت صراحةً ("لا تعمل refactor شامل"). إن أردت معالجتها لاحقًا، أرسل قائمة الأخطاء الفعلية (نسخ/لصق من مخرجات `npm run lint` عندك) وسأعالجها بجولة منفصلة ومحصورة.

## P1 — تؤثر على التشغيل الحقيقي

- **Student Mode Security Boundary**: يستخدم JWT ولي الأمر نفسه؛ استدعاء Supabase REST API مباشرة (متجاوزًا التطبيق) نظريًا ممكن لمستخدم تقني متقدّم. الإغلاق الكامل يحتاج هوية Auth مستقلة للطالب — قرار معماري يحتاج إذنًا صريحًا منفصلًا عن أي جولة تطوير عادية.
- **6 من 9 صفحات `/admin/*` الفرعية لا تزال placeholders**: `students`, `parents`, `sessions`, `programs`, `payments`, `reports`. الإدارة اليومية الفعلية ستحتاجها قبل تشغيل حقيقي واسع (وليس Pilot محدود).
- **لا واجهة إدارية لتعليم `sessions.makeup_eligible`**: الحقل موجود ويعمل، لكن تفعيله حاليًا يتطلب تعديلًا مباشرًا في قاعدة البيانات، لا زر في `/admin`.
- **لا واجهة إدارية لإدخال `cohorts.meeting_url`**: نفس النقطة — الحقل موجود ويُستخدم فعليًا، لكن إدخاله DB-direct فقط.

## P2 — تحسين مهم بعد الـPilot

- **`subscription_pauses` بلا أتمتة لحالتَي `active`/`completed`**: `requested/approved/rejected` تعمل بالكامل وتمدّد `renewal_date` بشكل صحيح فور الاعتماد (الأثر المالي سليم)؛ لكن لا يوجد Scheduler/cron في هذه المعمارية Next.js-only لتحويل الحالة تلقائيًا مع مرور الوقت — تبقى الحالة "approved" دون تحديث نصي لاحق.
- **مراجعة UX/Responsive فعلية على أجهزة حقيقية** (375/390/430px، iPad) — التصميم مبني على مبادئ Mobile-first لكن لم يُعايَن بصريًا على شاشات فعلية.
- **واجهة Desktop مخصَّصة لمساحة الطالب** — تعمل حاليًا بنفس تصميم الجوال على عرض متمركز.
- **Formulas الـScorecard قابلة للنقاش**: عتبات `Healthy/Attention/Critical` (70%/90%، إلخ) قيم أولية معقولة، تحتاج مراجعة تشغيلية فعلية بعد بيانات Pilot حقيقية.

## P3 — Nice to Have

- **Child PIN / Quick Access**: البنية جاهزة معماريًا (`student_mode_sessions` لا تفترض آلية دخول واحدة) لكن لم تُبنَ.
- **Independence Ladder**: فكرة مقترحة سابقًا لتصعيد تدريجي في واجهة الصفوف 4-6 مبني على استقلالية فعلية متصاعدة، بدل ثبات حسب الصف فقط — تستحق نقاشًا منتجًا منفصلًا.
- **رقم Teacher Score مجمَّع واحد**: مؤجَّل عمدًا حتى تتوفر Policy أوزان واضحة (قرار منتج، وليس نقصًا تقنيًا).
- **Split View لصفحات الرسائل على Desktop** بدل نمط قائمة→صفحة الحالي.

# Parent Experience V1 — Report

## ROOT CAUSE OF DUPLICATE SESSIONS
`/api/payment/confirm` توليد جلسات شهر كامل عند تفعيل كل اشتراك، بحماية idempotency واحدة فقط:
"لا تُعِد التوليد لنفس الاشتراك إن كان `active` بالفعل". لكن عندما **طفلان مختلفان (اشتراكان
مختلفان) يشتركان في نفس المجموعة** (نفس `cohort_id` — سيناريو متوقَّع تمامًا أثناء اختبار
Pilot المتكرر لنفس المجموعات المزروعة)، كل تأكيد دفع منفصل يُولِّد شهرًا كاملًا من جلسات تلك
المجموعة من جديد. **لا يوجد قيد UNIQUE على `(cohort_id, starts_at)`** كان يمنع هذا من الأساس —
هذا يفسّر بدقة العرض المُلاحَظ: تواريخ متعددة مختلفة (8، 13، 15 سبتمبر، 4، 6 أكتوبر) كلها
مكرَّرة **معًا** دفعة واحدة، لا تاريخ واحد عشوائي.

## كيف تم إصلاح التكرار (3 طبقات)
1. **Migration** (`supabase/migrations/20260908_fix_duplicate_sessions.sql`): حذف آمن للتكرار
   الحالي — يُبقي صفًا واحدًا لكل `(cohort_id, starts_at)`، **ولا يحذف أي صف مرتبط فعليًا بحضور
   أو تقرير جلسة أو رصيد تعويض** (فحص صريح قبل كل حذف).
2. **قيد UNIQUE دائم** على `sessions(cohort_id, starts_at)` — يمنع التكرار من الأساس مستقبلًا،
   بصرف النظر عن أي مسار كود.
3. **إصلاح الكود المُولِّد** (`/api/payment/confirm`): `insert` → `upsert(..., {onConflict:
   "cohort_id,starts_at", ignoreDuplicates:true})` — الاشتراك الثاني على نفس المجموعة يتجاهل
   الجلسات الموجودة فعليًا بدل تكرارها أو الفشل بخطأ قيد.
4. **Dedup دفاعي إضافي** في طبقة العرض (`/parent/schedule/page.tsx`) — تصفية بمفتاح
   `cohort_id:starts_at` كحماية ثانية فقط، موثَّق بوضوح في الكود أنه **ليس بديلًا** عن الإصلاح
   الحقيقي أعلاه.

## كيف يعمل Arabic Relative Time
`src/lib/arabic-time.ts` — `formatRelativeSessionTime()`: أقل من دقيقة → "أقل من دقيقة"، أقل من
ساعة → "٪n دقيقة"، أقل من 24 ساعة → صياغة عربية صحيحة نحويًا (ساعة/ساعتين/٣-١٠ ساعات/أكثر)، بعد
24 ساعة → "غدًا، الوقت" أو "اليوم الاسم التاريخ، الوقت" بدل عدّاد ساعات ضخم. **لا ثواني إطلاقًا**
— هذا هو الإصلاح المباشر لمشكلة "04:59:30 / 124:59:30 / 676:59:30" التي كانت تأتي من
`SessionJoinButton.tsx` القديم (دالة `formatCountdown` كانت تحوّل أي فارق زمني، مهما طال، إلى
HH:MM:SS خام بلا حد أعلى). أُعيد بناء ذلك المكوّن ليعتمد فقط على حالتين فعليتين (يمكن الدخول /
انتهت)، وتُرك النص الزمني النسبي بالكامل لـ`NextSessionCard` (تحديث كل 30 ثانية، لا كل ثانية).
كل الحسابات تُجرى بتوقيت `Asia/Riyadh` عبر `Intl.DateTimeFormat` صراحة.

## مصدر بيانات Weekly Summary
حساب حي من `sessions` (ضمن حدود الأسبوع الحالي بتوقيت الرياض: الأحد 00:00 حتى السبت) +
`attendance` لعدد الحضور الفعلي. **لا بيانات وهمية** — إن لم توجد جلسات هذا الأسبوع، القسم كله
يعرض "لا توجد جلسات هذا الأسبوع بعد" بدل أصفار مُصطنَعة.

## مصدر بيانات Child Journey
أحدث صف حقيقي من `daily_pulse_reports` لأول طفل (نفس مصدر بيانات `/parent` الرئيسية). "آخر
تحديث" مُشتق من `tasks_completed`/`readiness_status` الفعليين، "القادم" من `remaining_review`
الفعلي أو حالة `readiness_status`. لا يوجد صف حقيقي بعد → Empty State المطلوب حرفيًا.

## Empty States المنفَّذة
لا اشتراك نشط → "لا يوجد اشتراك نشط حاليًا" + CTA "استكشف خُطى" • لا جلسات قادمة → رسالة هادئة
في `NextSessionCard`/`UpcomingSessions` • لا بيانات رحلة → نص الاستقبال المطلوب حرفيًا • خطأ تحميل
→ "تعذّر تحميل بياناتك الآن" + "إعادة المحاولة" (بلا كشف تفاصيل الخطأ التقني).

## Pilot Auth — التحسين المطلوب في البند 13
نفّذته: `/api/payment/confirm` يضع الآن كوكي HttpOnly قصيرة العمر (30 دقيقة، بيئة تطوير فقط)
تحمل `subscriptionId` الذي فُعِّل للتو. `resolveParentContext()` يتحقق منها **أولًا** (مع تأكيد
أن الوالد المرتبط لا يزال بلا `user_id`) قبل اللجوء لـ"أحدث ولي أمر ضيف" العام. هذا يربط جلسة
Pilot تحديدًا بالاشتراك الذي أنشأه المستخدم في هذه الرحلة نفسها، لا بأي عميل تجريبي آخر. الاحتياط
العام (أحدث ضيف) يبقى فقط لجلسات Pilot قديمة بلا كوكي مرتبطة — **P2 وليس P1 بعد الآن**.

## اللغة العربية — تصحيحات
أصلحت ظهور "Pilot" بالإنجليزية داخل شارة `/parent` (كانت من جولة سابقة) إلى "وضع تجريبي — بيانات
تجريبية" حرفيًا. أضفت `toParentFacingProgramName()` يستبدل "Focus Room" بـ"غرفة التركيز" في كل
مكان يظهر فيه اسم برنامج للوالد — **بدون تعديل بيانات `cohorts.title` نفسها في قاعدة البيانات**
(تحويل عرض فقط، البيانات لم تُغيَّر إطلاقًا).

## الملفات الجديدة (9)
`src/lib/arabic-time.ts` • `src/app/parent/schedule/{ParentDashboardHeader,NextSessionCard,
WeeklySummary,ChildJourney,UpcomingSessions}.tsx` • `supabase/migrations/20260908_fix_duplicate_sessions.sql`

## الملفات المعدَّلة (6)
`src/app/parent/schedule/page.tsx` (إعادة بناء كاملة) • `src/app/parent/schedule/
SessionJoinButton.tsx` (إزالة العدّاد الخام + وعي مسبق بـ`meeting_url`) • `src/app/api/
payment/confirm/route.ts` (upsert بدل insert + كوكي Pilot) • `src/lib/pilot-parent.ts`
(أولوية كوكي الاشتراك) • `src/app/parent/page.tsx` (نص "Pilot" → عربي) • `supabase/schema.sql`
(قيد UNIQUE منعكس في التعريف النهائي) • `src/app/globals.css` (إضافات بصرية طفيفة فقط)

## نتيجة npm run build
**لم يُختبر فعليًا بنجاح** — نفّذته: `403 Forbidden` على `npm install` (نفس القيد الشبكي من كل
جولة في هذا المشروع، بيئة بلا وصول إنترنت). فحصت توازن الأقواس عبر كل ملفات TS/TSX (سليم) وتحققت
يدويًا من صحة كل استيراد مقابل التصدير الفعلي — لكن هذا ليس بديلًا عن Build حقيقي. يحتاج تشغيلك
على بيئة بوصول شبكة فعلي.

## P1/P2 متبقٍ
- **P1**: قيد UNIQUE الجديد سيفشل عمدًا إن بقيت أي مجموعة `(cohort_id, starts_at)` مكرَّرة
  ببيانات حضور/تقارير حقيقية مرتبطة بأكثر من نسخة بعد خطوة التنظيف الآمن — يحتاج مراجعة يدوية
  لتلك الحالة تحديدًا إن ظهرت عند تطبيق الـMigration فعليًا.
- **P2**: الاحتياط العام لربط Pilot ("أحدث ولي أمر ضيف") لا يزال موجودًا لجلسات قديمة بلا كوكي
  اشتراك — نطاقه محدود (بيئة Pilot فقط، لا جلسة Supabase حقيقية) لكنه ليس مثاليًا 100%.
- **P2**: لم أختبر بصريًا فعليًا على 375/390/430/768px (لا متصفح متاح هنا) — الـCSS مبني على
  أنماط `.dashcard`/`.kpi`/`.session-row` المُختبَرة بصريًا في جولات سابقة، لكن التركيبة الجديدة
  (Hero Card + Grid) لم تُعايَن حيًا.

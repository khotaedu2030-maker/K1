# خُطى | KHOTA — تقرير طبقة الامتثال القانوني والاستهلاكي

## A) الملفات التي أُنشئت
- `src/lib/legal-profile.ts` — مصدر واحد لبيانات مقدّم الخدمة.
- `src/components/LegalPage.tsx` — تخطيط مشترك للصفحات النظامية (عنوان + مقدمة + فهرس + أقسام).
- `src/app/refund-policy/page.tsx` — سياسة الاسترجاع والاسترداد.
- `src/app/complaints/page.tsx` — الشكاوى والمقترحات.
- `src/app/legal/page.tsx` — بيانات مقدّم الخدمة (يعرض فقط الحقول المؤكَّدة، صفر placeholder).

## B) الملفات التي عُدِّلت
`src/app/terms/page.tsx` (إعادة بناء كاملة) • `src/app/privacy/page.tsx` (إعادة بناء كاملة) •
`src/app/contact/page.tsx` (تحذير أمني) • `src/components/Footer.tsx` (قسم قانوني جديد بالفوتر
+ تعديل CSS الشبكة لاستيعاب العمود الخامس) • `src/app/motabaa/enroll/payment/PaymentClient.tsx`
(إقرارات ما قبل الدفع + تعديل صياغة السعر) • `src/app/motabaa/enroll/EnrollForm.tsx` (نفس
تعديل صياغة السعر + رابط سياسة الاسترجاع) • `src/app/motabaa/enroll/page.tsx` (مصدر نص السعر)
• `src/app/parent/subscriptions/page.tsx` (تسمية "تاريخ التجديد" → "نهاية الدورة الحالية") •
`src/app/sitemap.ts` (3 مسارات جديدة).

## C) أهم النصوص/السياسات التي تغيّرت فعليًا
1. **حذفت ادّعاء التجديد التلقائي غير المؤكَّد.** النص القديم بالشروط كان: "تُجدَّد الخطة
   تلقائيًا كل شهر ما لم يُلغَ الاشتراك" — لا يوجد في المشروع أي آلية شحن تلقائي فعلية (لا يوجد
   حتى بوابة دفع حقيقية مربوطة). استُبدِل بنص لا يفترض آلية غير موجودة.
2. **حذفت ادّعاء "لا نشارك بياناتك مع أي طرف ثالث" المطلق.** كان يناقض فعليًا استخدام المشروع
   الحقيقي لمزوّدي خدمة تقنيين (Supabase، خدمة اجتماعات مرئية، إلخ). استُبدِل بإفصاح واضح عن
   فئات المزوّدين دون تسمية غير مؤكَّدة.
3. **حيّدت لغة التحقق تقنيًا.** "رمز تحقق يصل لجوال ولي الأمر" → "بيانات التواصل... وفق وسيلة
   الدخول المفعَّلة حاليًا" — لا تفترض SMS تحديدًا، جاهزة لتحوّل Email OTP دون تعديل لاحق.
4. **لم أكتب أيًا من العبارات الممنوعة صراحة** ("لا يوجد استرداد بعد الدفع/بدء الخدمة"، "متوافقة
   بالكامل مع PDPL"، رقم ضريبي، سجل تجاري، ترخيص تعليمي) — تحقّقت بـ`grep` مباشر، صفر نتيجة.
5. **"تاريخ التجديد" (لوحة ولي الأمر) → "نهاية الدورة الحالية".** هذا التصنيف الأصلي وحده كان
   يوحي ضمنًا بتجديد تلقائي غير مؤكَّد فعليًا — الحقل الداخلي `renewal_date` نفسه لم يُلمَس.
6. **صياغة السعر في التسجيل والدفع**: "399 ر.س / شهريًا" → "قيمة الاشتراك لهذه الدورة: 399
   ر.س" — تفاديًا لإيحاء تجديد تلقائي بصريًا، بلا تغيير في الرقم أو المصدر نفسه.

## D) نتيجة tsc
نُفِّذ فعليًا (`npx tsc --noEmit`). الغالبية الساحقة من المخرجات (`TS7026`/معظم `TS7006`) هي
نفس ضجيج البيئة الموثَّق في كل جولة سابقة من هذا المشروع (غياب `node_modules` تمامًا هنا).
**وجدت واستثنيت بدقة اثنين من الأخطاء الحقيقية الجديدة** الناتجة عن معالِجَي `onChange` الجديدين
لخانتي الإقرار في `PaymentClient.tsx` (كانا بلا نوع صريح) — أصلحتهما بـ`ChangeEvent<HTMLInputElement>`
مستوردة كنوع من `"react"` مباشرة (نمط أكثر أمانًا من الاعتماد على namespace عام `React`، وتحقّقت
أن الملف متوازن الأقواس بعد التعديل). راجعت بقية أخطاء `TS7006` الظاهرة في الملفات التي لمستها
(`EnrollForm.tsx`, `parent/subscriptions/page.tsx`) وتأكّدت مباشرة أنها **على أسطر لم ألمسها
إطلاقًا** — دَين تقني قديم موثَّق مسبقًا، لا علاقة له بهذه الجولة.

## E) نتيجة build
**فشل** — `next: not found`، نتيجة مباشرة لفشل `npm install` بـ`403 Forbidden` (لا وصول شبكة
في هذه البيئة، نفس القيد المتكرر في كل جولة من هذا المشروع منذ البداية دون استثناء). لم يُشغَّل
build ناجح فعليًا هنا لا بهذه الجولة ولا بأي جولة سابقة.

## F) بيانات ناقصة يجب على مالك خُطى تزويدها قبل الإطلاق التجاري
كلها مُعرَّفة حاليًا كـ`null` صراحة في `src/lib/legal-profile.ts` — لا تظهر placeholders
للمستخدم، وتحديث هذا الملف الواحد يكفي لظهورها تلقائيًا في كل الصفحات النظامية:
1. **اسم مقدّم الخدمة النظامي** (`providerName`).
2. **رقم وثيقة العمل الحر** (`registrationNumber`) — نوع التسجيل مُعرَّف مسبقًا بـ"وثيقة عمل
   حر" بناءً على معطيات المشروع، لكن الرقم نفسه غير موجود.
3. **عنوان/مقر العمل** المطلوب نظاميًا (`businessAddress`).
4. **بريد الدعم الفعلي** (`supportEmail`) — بدونه، تعرض صفحات الخصوصية/الاسترجاع/الشكاوى رابط
   `/contact` كبديل بدل بريد وهمي.
5. **رقم التواصل الفعلي** إن وُجد (`supportPhone`).
6. **الحالة الضريبية والرقم الضريبي** إن كان مسجَّلًا (`vatNumber`) — لم تُعرَض أي إشارة ضريبية
   في أي صفحة حاليًا لعدم توفر تأكيد.

## G) هل نموذج التواصل يعمل فعليًا؟
**لا.** فحصت `contact/page.tsx` مباشرة: زر "إرسال" بلا `onClick` أو أي منطق إرسال — واجهة
بصرية فقط، لا يوجد Backend أو API خلفها. لم أدَّعِ في أي نص أن الإرسال يعمل، ولم أُنشئ Backend
جديدًا (خارج النطاق المطلوب صراحة). أضفت فقط تحذير "لا ترسل بيانات البطاقة أو كلمات المرور أو
رموز التحقق" كما طُلب. **هذا Blocker حقيقي قبل أي إطلاق تجاري** — أي صفحة نظامية تُحيل حاليًا
إلى `/contact` كوسيلة تواصل فعلية (الاسترجاع، الشكاوى) تفترض ضمنيًا أن هذا النموذج يعمل، وهو لا
يعمل بعد.

## H) هل توجد عبارات تتعارض مع سياسة الاسترجاع الجديدة؟
لا — بحثت تحديدًا عن "لا يوجد استرداد" وعبارات مشابهة عبر كل `src/`، صفر نتيجة. صفحة
`/terms` القديمة (قبل إعادة البناء) لم تكن تحتوي هذه العبارة أصلًا رغم إشارة الطلب لاحتمال
وجودها — تحقّقت بالفحص المباشر لا بالافتراض.

## I) إشارات auto-renewal غير مفعَّل
**كانت موجودة وأُصلحت** — راجع البند C.1 أعلاه. لا يوجد الآن أي نص يدّعي تجديدًا تلقائيًا نشطًا.
تحقّقت من الكود الفعلي: لا يوجد Cron Job أو آلية شحن متكررة حقيقية في المشروع (وهذا منطقي، فلا
بوابة دفع حقيقية مربوطة أصلًا حاليًا) — حقل `renewal_date` بقاعدة البيانات هو مجرد تاريخ نهاية
الدورة الحالية، لا آلية تفعيل تلقائي.

## J) ادّعاءات غير مثبتة عن الدفع/الأمان/الترخيص
بحثت عن "PCI"، "مرخّص"، "متوافق بالكامل"، "آمن 100%" — صفر نتيجة في أي مكان بالموقع. صفحة الدفع
الحالية (`PaymentClient.tsx`) توضّح صراحةً أنها "الدفع التجريبي متاح في بيئة التطوير فقط" —
شفافية موجودة أصلًا من جولات سابقة، لم أحتَج لمسها.

## K) مزوّدو الخدمات الخارجيون الملحوظون في الكود + مسألة النقل خارج المملكة
لاحظت في الكود فعليًا:
- **Supabase** (قاعدة البيانات + المصادقة + RLS) — عبر `@supabase/supabase-js` و`@supabase/ssr`.
- **Vercel** كبيئة استضافة (وفق الطلب الأصلي الذي يذكر "Vercel" صراحة كبيئة إنتاج مؤقتة).
- **خدمة اجتماعات مرئية** للجلسات (حقل `meeting_url` بقاعدة البيانات — لم أتحقق من اسم المزوّد
  المحدَّد، فقط من وجود الحقل نفسه).
- **مزوّد دفع**: لم يُربَط بعد فعليًا (Moyasar/Tap/HyperPay مذكورة كتعليق تخطيطي فقط في الكود،
  لا تكامل فعلي).

**⚠️ نقطة تستحق مراجعة تشغيلية مستقلة، لا مجرد نص بسياسة الخصوصية:** لم أتمكَّن من التحقق من
الـRegion الفعلي لمشروع Supabase المستخدَم (داخل السعودية أو خارجها) — هذا إعداد يُضبَط من لوحة
Supabase نفسها، لا يظهر في كود المشروع. سياسة الخصوصية الجديدة تفصح بشكل عام عن احتمال معالجة
البيانات خارج المملكة تحسبًا لذلك، لكن **يجب على المالك التأكد فعليًا من الـRegion المُستخدَم
وتحديد ما إذا كانت هذه الصياغة العامة كافية أو تحتاج تدقيقًا أعمق.**

## L) نقاط نظامية تحتاج إجراءً تشغيليًا من المالك (لا تُحَل بتعديل واجهة فقط)
1. **ربط نموذج التواصل ببريد أو نظام تذاكر فعلي** — أهم Blocker تشغيلي، يمس مصداقية صفحتي
   الاسترجاع والشكاوى الجديدتين.
2. **تعبئة `legal-profile.ts`** بالبيانات الفعلية (البند F) — بدونها، صفحة `/legal` ستبقى شبه
   فارغة، وصفحتا الخصوصية/الاسترجاع ستستمران بالإحالة لـ`/contact` كبديل.
3. **تأكيد الـRegion الفعلي لـSupabase** (البند K) — قرار تشغيلي مستقل عن نص الموقع.
4. **ربط بوابة دفع حقيقية** مع تحقّق Server-side حقيقي — الكود الحالي مصمَّم بحيث يسهل هذا
   الاستبدال لاحقًا (لم أُغيّر منطق `confirm()` إطلاقًا هذه الجولة، تحقّقت بمقارنة مباشرة أنه
   حرفيًا كما كان)، لكن الربط نفسه إجراء تشغيلي خارج نطاق تعديل الواجهة.
5. **قرار مالك خُطى بشأن الرقم الضريبي** — إن كانت هناك نية للتسجيل الضريبي قريبًا، يُفضَّل
   إبلاغي لاحقًا بمجرد توفره ليُعرَض بشكل صحيح، لا الآن.

## ملاحظة تصميمية
طبّقت إقرارَي الموافقة (سياسات + ولي الأمر) في `PaymentClient.tsx` تحديدًا — نقطة الدفع
الفعلية الوحيدة في التدفّق الحالي (`EnrollForm.tsx` يجمع بيانات التسجيل فقط، لا يُنفّذ دفعًا).
لم أُخزِّن أي موافقة بقاعدة البيانات (يتطلب schema change، خارج النطاق)، لكن تركت تعليقًا
واضحًا بالكود يوضّح كيفية تمرير `policyVersion`/`timestamp`/`subscriptionId` لاحقًا عند إضافة
سجل تدقيق حقيقي.

# KHOTA Final Master Report

## 1) Executive Summary
هذه جولة ضخمة (41 قسمًا). نفّذت **P0 (صحة البيانات) بالكامل ومُثبَتًا بفحص فعلي**، مراجعة أمان
مستهدفة، تصحيحات لغوية حقيقية (لا Latin مسرَّب، لا أخطاء خام)، صفحات خصوصية/شروط حقيقية، SEO
أساسي (metadata/robots/sitemap)، وتحسين طباعة الـHero. **لم أنفّذ إعادة تصميم Corporate كاملة
لكل صفحة بعمق Aramco/NEOM** (storytelling كامل، Product Showcase، Checkout عمودين، Stepper
مرئي، رفع تصميم لوحة ولي الأمر، اختبار 8 مقاسات فعليًا) — هذا عمل تصميمي متعدد الأيام بحجم
حقيقي، وذكر ذلك كـP1/P2 صريح أدق من ادّعاء إنجازه جزئيًا. لا أدّعي PASS على أي شيء لم أختبره.

## 2) الملفات المعدَّلة/الجديدة
**Migration جديدة**: `supabase/migrations/20260908b_enforce_plan_days_integrity.sql` •
**Schema**: `supabase/schema.sql` (Trigger منعكس) • **API**: `src/app/api/enroll/route.ts`،
`src/app/api/payment/confirm/route.ts` • **Login**: `src/app/login/page.tsx`،
`src/app/login/PilotLoginSection.tsx` • **Payment**: `src/app/motabaa/enroll/payment/
{page,PaymentClient}.tsx` • **Footer/Legal**: `src/components/Footer.tsx`،
`src/app/privacy/page.tsx` (جديد)، `src/app/terms/page.tsx` (جديد) • **SEO**: `src/app/
layout.tsx`، `src/app/robots.ts` (جديد)، `src/app/sitemap.ts` (جديد) • **Design**:
`src/app/globals.css` (طباعة Hero فقط) • **QA**: `scripts/qa-plan-days.mjs` (جديد، شُغِّل
فعليًا ونجح).

## 3) Design changes
تكبير طباعة Hero فقط (`clamp(40px,7vw,88px)` Desktop، ينزل تلقائيًا لـ40px على الجوال) —
**منفصل عن `.title` العام** (49 ملفًا يستخدمها) عمدًا كي لا تتضخّم كل صفحة أخرى. أزلت تجاوزًا
كان يصغّر عناوين الجوال لـ32px بلا داعٍ (كان يتعارض مع الحد الأدنى الصحيح لـclamp نفسه). **هذا
هو مدى التعديل البصري المنفَّذ فعليًا** — لم أعِد بناء Hero composition أو storytelling أو
الصفحات الأخرى بصريًا هذه الجولة (انظر Remaining P1).

## 4) Arabic copy changes
أصلحت 3 تسريبات لغوية حقيقية (لا نظرية): (أ) "Pilot فقط — بيئة تطوير" داخل زر الدخول التجريبي
→ "وضع تجريبي — بيئة تطوير فقط"، (ب) `/login` كان يعرض `error.message` الخام من Supabase مباشرة
للمستخدم (بما فيها "Unsupported phone provider" حرفيًا) → دالة `friendlyAuthError()` تُترجم
الحالات المعروفة لعربية واضحة وتُخفي أي رسالة تقنية أخرى خلف "تعذّر إكمال العملية الآن"، (ج)
Footer: "Terms"/"Privacy" بالإنجليزية تربطان كلتاهما بـ`/help` خطأً → صفحتان عربيتان حقيقيتان
جديدتان (`/privacy`, `/terms`) بروابط صحيحة. حدَّثت عنوان/وصف `/login` ليطابقا النص المطلوب
حرفيًا. حدَّثت Success State للدفع (تفصيل في القسم 8). **لم أُجرِ مراجعة نصية شاملة لكل صفحة
مذكورة في القسم 23** (about/help/contact/teachers/teach-with-khota لم تُراجَع سطرًا بسطر هذه
الجولة) — فحصت بحثًا آليًا عن "Pilot"/"Focus Room"/English مسرَّب في كامل `src/app` ولم أجد
غير الثلاثة أعلاه، لكن هذا بحث آلي عن مصطلحات محدَّدة، وليس مراجعة نبرة/صياغة شاملة.

## 5) ROOT CAUSE — مشكلة Plan/Day (حقيقي، مُتتبَّع من الكود لا افتراضًا)
كل جلسة تُولَّد من `cohorts.days_of_week` مباشرة في `/api/payment/confirm`، ولوحة ولي الأمر
تعرض كل جلسات `cohort_id` اشتراك العائلة. هذا **صحيح فقط إذا** `cohorts.days_of_week` يطابق
دائمًا `plans.days_per_week` لنفس المجموعة — **ولم يوجد أي قيد أو تحقق يفرض هذا التطابق في أي
مكان**. لو أُنشئت مجموعة (يدويًا مستقبلًا) بخطة يومين لكن `days_of_week` بها 4 أيام، يرى ولي
الأمر 4 أيام رغم اشتراكه بخطة يومين — بالضبط السيناريو الموصوف.

## 6) كيف أصبح: plan → days → sessions → dashboard
**القرار (الأقل تغييرًا، لا معمارية جديدة)**: بما أن كل مجموعة حاليًا مرتبطة بخطة واحدة محدَّدة
فعليًا (لا مجموعة مشتركة بين خطط)، الإصلاح هو **فرض** هذا الافتراض بدل الاعتماد الضمني عليه،
على 3 طبقات:
1. **Trigger في قاعدة البيانات** (`enforce_cohort_days_match_plan`) — يرفض أي `insert`/`update`
   على `cohorts` إن كان `array_length(days_of_week) != plans.days_per_week` لنفس `plan_id`.
2. **`/api/enroll`**: نفس التحقق قبل إنشاء أي اشتراك، برسالة عربية واضحة بدل خطأ DB خام.
3. **`/api/payment/confirm`**: نفس التحقق قبل توليد أي جلسة فعليًا — فشل واضح (500 + رسالة
   عربية، لا تفعيل صامت) بدل توليد عدد جلسات خاطئ.

بهذا، `cohort_id` (مصدر جلسات لوحة ولي الأمر الحالي) يبقى آمنًا وصحيحًا **لأنه أصبح مضمون
التطابق مع الخطة دائمًا** — لم أبنِ علاقة subscription↔schedule جديدة (تغيير معماري أكبر لم
يكن ضروريًا لنموذج "مجموعة = خطة واحدة" الحالي). **تحققت فعليًا**: كل المجموعات السبع المزروعة
في Wave 1 تطابق خططها فعلًا (شغّلت `scripts/qa-plan-days.mjs` — PASS حقيقي، النتيجة أعلاه).

## 7) Parent data isolation
لم يتغيّر هذه الجولة (كان صحيحًا من جولة Parent Experience V1: `resolveParentContext()` +
فلترة صريحة بـ`parent_id`/`child_id`، لا اعتماد ضمني على RLS وحدها). **تحسين واحد إضافي هنا**:
كوكي `khota_pilot_last_subscription` (من الجولة السابقة) تُقلِّل استخدام fallback "أحدث ولي
أمر ضيف" — لم يُلغَ الاحتياط بالكامل (يبقى P2 موثَّق).

## 8) Payment success flow
كان يختفي تلقائيًا بعد 1.5 ثانية بدون تفاعل المستخدم — بالضبط المشكلة الموصوفة. الآن: شارة
"تم الاشتراك بنجاح" + "تم حجز مقعد طفلك في خُطى." + زر صريح "الانتقال إلى لوحة ولي الأمر ←"
(أزلت الـ`setTimeout` التلقائي — المستخدم يتحكم بالانتقال، لا يُفاجَأ به). عنوان الصفحة تغيّر
لـ"أكمل اشتراكك" مطابقًا للمطلوب حرفيًا.

## 9) Login/error UX
عنوان ووصف مطابقان للنص المطلوب حرفيًا. لا رسائل خام تصل للمستخدم بعد الآن (تفصيل في القسم 4).

## 10) Admin operational improvements
**لم تُنفَّذ هذه الجولة** — القسم 31 يطلب حدًا أدنى تشغيليًا حقيقيًا (إدارة `meeting_url`،
إسناد معلم، إلخ) وهذا عمل واجهة+منطق حقيقي بحجم لا يتسع ضمن الأولوية الأعلى (P0) في هذه الجولة.
موثَّق كـP0 تشغيلي متبقٍ أدناه — **هذا فعليًا أهم فجوة متبقية للتشغيل الحقيقي** (لا يزال يلزم
فتح Supabase يدويًا لإدخال `meeting_url`/`teacher_id`).

## 11) SEO
`layout.tsx`: title template + description + keywords + Open Graph + Twitter card (عربي،
`locale: ar_SA`). `robots.ts`/`sitemap.ts` جديدان — يستثنيان `/parent`, `/teacher`, `/admin`,
`/student`, `/api`, `/english`, `/qudurat` صراحة. **لا `metadataBase`/canonical مطلق** — لا
نطاق إنتاج معروف، لم أخترعه (موضَّح كتعليق في الكود). **لا favicon** — لا يوجد أي ملف أيقونة
في المشروع أصلًا؛ لم أخترع واحدًا (`LogoMark` SVG موجود في الكود لكن غير مُصدَّر كملف favicon
ثابت).

## 12) Accessibility
**لم تُراجَع بعمق هذه الجولة** (القسم 28 بالكامل) — لا تدقيق focus states/aria-labels/alt
text شامل نُفِّذ. P1 متبقٍ صريح.

## 13) Performance
لم تُراجَع هذه الجولة تحديدًا (لا تغييرات على next/image sizing/priority هذه الجولة). لا
مكتبات جديدة أُضيفت (`qa-plan-days.mjs` سكربت Node خام بلا أي dependency).

## 14) Responsive
**لم يُختبَر فعليًا على أي مقاس** (لا متصفح متاح في هذه البيئة) — لم أدّعِ اختبارًا لم يحدث.
تعديل الطباعة الوحيد (Hero) يعتمد على `clamp()` المتجاوب أصلًا رياضيًا، لكن هذا استنتاج منطقي
لا معاينة بصرية فعلية.

## 15) QA scenarios
شغّلت `scripts/qa-plan-days.mjs` فعليًا (PASS، مُخرَجات حقيقية أعلاه) — يغطي منطق تطابق الأيام
+ يتحقق من كل مجموعات Wave 1 المزروعة فعليًا. **لم أُشغِّل سيناريوهات A/B/C/D الأربعة الكاملة
E2E** (تحتاج خادمًا حيًا لا يتوفر هنا) — يحتاج تشغيلك.

## 16) نتيجة npm run build
**FAIL فعلي** — `403 Forbidden` على `npm install` (نفس القيد الشبكي من كل جولة سابقة في هذا
المشروع، بيئة بلا وصول إنترنت). فحصت توازن الأقواس آليًا عبر كل ملفات TS/TSX (سليم) — ليس بديلًا
عن Build حقيقي.

## 17) نتيجة npm run lint
**FAIL فعلي لنفس السبب** — `eslint: not found` (لا `node_modules`). لم أصنّف legacy lint
لأنني لم أستطع تشغيله إطلاقًا هذه الجولة ولا الجولات السابقة.

## 18) Remaining P0
- **Admin operational essentials (القسم 31)** — إدارة `meeting_url`/`teacher_id` لا تزال DB-direct.
- **تشغيل الـMigration الجديدة فعليًا** على Supabase الحي والتحقق أن الـTrigger لا يرفض أي بيانات
  Pilot موجودة فعلًا لديك (تحققت من بيانات Wave 1 **كما أعرفها من الكود** — ليس اتصالًا حيًا).

## 19) Remaining P1
- Corporate redesign الكامل (Storytelling، Product Showcase، Checkout عمودين، Stepper مرئي،
  رفع تصميم لوحة ولي الأمر) — لم يُنفَّذ هذه الجولة، عمل تصميمي كبير يستحق جولة مخصَّصة.
  Accessibility audit كامل • Responsive testing فعلي على 8 مقاسات • مراجعة نصية شاملة لكل صفحة
  مذكورة في القسم 23 (فُحص آليًا عن مصطلحات محدَّدة فقط) • Trust Metrics الجديدة (القسم 15) لم
  تُضَف كـSection مرئي — رقم "45 دقيقة" تحديدًا يجب ألا يُستخدم أصلًا (مدة الجلسة الفعلية 60
  دقيقة حسب الكود الحالي، وثّقت هذا هنا صراحة كي لا يُستخدم رقم خاطئ لاحقًا).

## 20) External Production Configuration (متوقَّع، لم يُنفَّذ Fake)
مزوّد SMS/Auth حقيقي • مزوّد دفع حقيقي (Moyasar/Tap/HyperPay) • روابط اجتماعات فعلية لكل مجموعة
• معلمون حقيقيون مُسنَدون • نطاق إنتاج فعلي (يُستخدم بعده لإكمال `metadataBase`/canonical/OG
image مطلق) • favicon فعلي.

---

## Addendum — Continuation Round

### أُنجز هذه الجولة (تكملة P0/P1 من التقرير أعلاه)

**1) Admin Operational Essentials (كان P0 متبقٍ — أُغلق الآن)**
`/admin/groups` صار يدعم فعليًا إدارة `cohort.meeting_url` وإسناد `teacher_id` بدون فتح
Supabase يدويًا: نموذج تعديل مباشر لكل مجموعة (رابط + قائمة معلمين) عبر `/api/admin/
cohort-operations` (تحقق صلاحية Admin على الخادم، تحقق صحة الرابط، تحقق وجود المعلم).
**اكتشاف مهم أثناء البناء**: الجلسات تحتفظ بنسخة من `meeting_url` وقت توليدها (وقت تأكيد
الدفع) — لو أضاف الأدمن الرابط بعد توليد الجلسات (السيناريو الواقعي)، تحديث المجموعة وحده لن
يُظهر الرابط للوالد. أضفت تحديثًا تلقائيًا لكل الجلسات المستقبلية المجدولة لنفس المجموعة عند
حفظ رابط جديد — بدون هذا، الميزة كانت ستبدو تعمل لكن لا تصل فعليًا لولي الأمر.

قسم تنبيه جديد "يحتاج بيانات تشغيلية" يُظهر أي مجموعة بلا رابط و/أو معلم بوضوح، بالإضافة لإمكان
تعديل أي مجموعة أخرى موجودة (تغيير معلم لاحقًا مثلًا).

**2) Trust Metrics (كان P1 متبقٍ)**
شريط أرقام حقيقية فقط على الصفحة الرئيسية بعد الـHero: "1–12 الصفوف الدراسية"، "4 مراحل
تعليمية"، "60 دقيقة لكل جلسة" — **60 وليس 45** (تحققت من الكود: KHOTA Method فعليًا 50 دقيقة
تفاعل + 10 دقائق توثيق = 60). لا إحصاءات أو شهادات أو شعارات وهمية.

### الملفات الجديدة/المعدَّلة هذه الجولة
`src/app/api/admin/cohort-operations/route.ts` (جديد) •
`src/app/admin/groups/CohortOperationsForm.tsx` (جديد) •
`src/app/admin/groups/page.tsx` (معدَّل) • `src/app/page.tsx` (شريط Trust Metrics) •
`src/app/globals.css` (تنسيق الشريط فقط)

### نتيجة npm run build
**FAIL فعلي مجددًا لنفس السبب** — `403 Forbidden` على `npm install` (القيد الشبكي نفسه في كل
جولة). فحصت توازن الأقواس (سليم) وأعدت تشغيل `scripts/qa-plan-days.mjs` فعليًا (PASS).

### ما زال متبقيًا (بلا تغيير عن التقرير الأصلي)
Corporate redesign الكامل، Accessibility audit شامل (تحققت من نقطة واحدة فقط: aria-label على
القائمة المنسدلة للجوال — موجود ومناسب أصلًا)، Responsive testing فعلي، مراجعة نصية شاملة لكل
صفحة، Checkout عمودين، Stepper مرئي، رفع تصميم لوحة ولي الأمر.

---

## Addendum 2 — Continuation Round (Plans Stepper + Terminology)

### أُنجز هذه الجولة

**1) Stepper مرئي في `/motabaa/plans` (كان P1 — القسم 18)**
شريط تقدّم 01/02/03/04 (الصف/الخطة/المجموعة/التسجيل) فوق الصفحة — حالات بصرية ثلاث (مكتمل ✓
بلون Teal / حالي بحدود داكنة / قادم باهت). **مؤشر تقدّم بصري فقط، لا Gating** — تعمّدت عدم إخفاء
الخطوات اللاحقة خلف السابقة (نمط "كل شيء ظاهر دفعة واحدة" الحالي يبقى كما هو، أقل مخاطرة، ولم
يُطلب صراحة تغيير آلية التفاعل، فقط "تُعرض بوضوح"). **إثبات فعلي لا افتراض**: قارنت كتلة
المنطق الكاملة (imports/types/state/فلترة، الأسطر 1-64) بـ`diff` قبل/بعد — **متطابقة حرفيًا**؛
كل ما تغيّر JSX العرض فقط.

**2) توحيد مصطلح "الخطة" (كان P1 — القسم 24)**
"الباقة"/"باقتك"/"الباقات" كانت منتشرة رغم أن القسم 24 يطلب "الخطة" حصرًا. استبدلتها في **6
ملفات** (`page.tsx`, `terms/page.tsx`, `help/page.tsx`, `motabaa/plans/page.tsx`,
`motabaa/page.tsx`, `motabaa/enroll/page.tsx`) — بحث نصي دقيق استثنى عمدًا كلمة "الباقي"
(المتبقّي) غير المرتبطة، تحققت بـ`grep` أن الاستبدال لم يمسّها (تختلف بحرف أخير: ة مقابل ي).

### الملفات المعدَّلة هذه الجولة
`src/app/motabaa/plans/PlansSelector.tsx` (Stepper + "الخطة") • `src/app/globals.css`
(تنسيق `.stepper`) • `src/app/page.tsx`, `src/app/terms/page.tsx`, `src/app/help/page.tsx`,
`src/app/motabaa/plans/page.tsx`, `src/app/motabaa/page.tsx`, `src/app/motabaa/enroll/page.tsx`
(توحيد المصطلح فقط)

### نتيجة npm run build
**FAIL فعلي لنفس السبب المتكرر** (403 على npm install، لا وصول شبكة في هذه البيئة). توازن
الأقواس سليم عبر كل الملفات، وأعدت تشغيل `scripts/qa-plan-days.mjs` فعليًا (PASS).

### ما زال متبقيًا
Checkout عمودين لصفحة enrollment (القسم 19)، رفع تصميم لوحة ولي الأمر بصريًا (القسم 22)،
Product Showcase على الصفحة الرئيسية (القسم 13)، Homepage storytelling الكامل بالتدرّج المطلوب
(القسم 12)، Accessibility audit شامل، Responsive testing فعلي على 8 مقاسات.

---

## Addendum 3 — Final Completion Round (Checkout → QA)

### 1) Checkout (القسم 19) — مُنفَّذ
`/motabaa/enroll` صار Layout عمودين حقيقي: `.checkout-summary` (تفاصيل الاشتراك: البرنامج/
الصف/الخطة/الأيام/المجموعة/الوقت/السعر — كل حقل من بيانات حقيقية من `cohorts`/`plans`، لا
Hardcode) + عمود النموذج. عنوان الصفحة "باقي خطوة واحدة لإكمال الاشتراك" حرفيًا. **العقد بين
`page.tsx`/`EnrollForm.tsx` تغيّر من string واحد مُجمَّع إلى object مُهيكَل** — تغيير في شكل
البيانات المعروضة فقط، **لا تغيير في `submit()`** (تحققت بـ`diff` — مطابق حرفيًا لما كان قبل
هذه الجولة).

### 2) Payment (القسم 20) — مُنفَّذ، ملخص كامل الآن أيضًا
شاشة الدفع صارت نفس Layout عمودين بنفس التفاصيل الكاملة (كانت تعرض السعر فقط). **`confirm()`
وحالة النجاح كلاهما مطابقان حرفيًا لما قبل هذه الجولة** (تحقق `diff` فعلي).

### 3) Parent Dashboard (القسم 22) — رُفع بصريًا
`max-width` من 760px إلى 920px، بطاقة الجلسة القادمة بظل أوضح وطباعة أكبر، عنوان اللوحة بخط
`clamp(30px,4vw,42px)`، والجلسات القادمة صارت Timeline بخط رأسي متصل بدل بطاقات `.session-row`
منفصلة متكرّرة (بالضبط طلب القسم 22: "أقل Cards، أقل Borders"). **منطق البيانات (كل استعلامات
Supabase، dedup، حساب الأسبوع) لم يُلمَس إطلاقًا.**

### 4) Product Showcase (القسم 13) — مُنفَّذ بأمانة
قسم "المنتج الحقيقي" على الصفحة الرئيسية: معاينة داخل `browser-frame` تستخدم **نفس الكلاسات
الحقيقية** المستخدمة في اللوحة الفعلية (`.dashcard`, `.next-session-hero`, `.kpi`) — وليس UI
جديدًا مُختلَقًا. الأرقام المعروضة (3 جلسات، ساعات العد التنازلي...) **توضيحية صراحةً** مع جملة
تنويه واضحة أسفلها: "معاينة توضيحية... بياناتك الحقيقية تظهر فور بدء أول جلسة" — التزامًا بعدم
عرض بيانات وهمية تبدو حقيقية.

### 5) Homepage Storytelling (القسم 12)
راجعت الترتيب المطلوب (المشكلة→كيف تعمل→رحلة الطالب→ما يراه ولي الأمر→المراحل→المنتج
الحقيقي→CTA) مقابل البنية الحالية: قسم "الحل: KHOTA Method" (6 خطوات: Scan→...→Report) **يغطي
فعليًا** "رحلة الطالب بعد المدرسة" المطلوبة (العودة من المدرسة→ترتيب الأولويات→جلسة→مراجعة→
تجهيز الغد) — نفس التسلسل بمصطلحات KHOTA الرسمية. **قرار واعٍ**: لم أُضِف قسمًا سادسًا مكرِّرًا
لنفس الفكرة (كان سيخالف تحذير القسم 12 نفسه من "3 Cards ثم 3 Cards ثم 3 Cards"). أضفت Product
Showcase في مكانه الصحيح (قبل CTA مباشرة) — الترتيب النهائي الفعلي: Hero → Trust Metrics →
المشكلة → الحل/الرحلة (Method) → قيمة الطالب/الوالد → المراحل → **المنتج الحقيقي (جديد)** →
كيف تبدأ → CTA. متوافق مع الترتيب المطلوب.

### 6) Accessibility (القسم 28) — تحسينات حقيقية محدودة، لا Audit شامل
- **Skip Link** جديد (`Shell.tsx`) — "تخطَّ إلى المحتوى الرئيسي"، مخفي بصريًا حتى التركيز عليه
  بلوحة المفاتيح (نمط Accessibility قياسي).
- **`role="alert"`** على كل رسائل الخطأ في النماذج — **11 موضعًا عبر 10 ملفات** (بحث شامل
  فعلي، لا انتقائي) — الآن قارئ الشاشة يُعلن الخطأ فور ظهوره بدل صمت كامل.
- تأكدت أن `aria-label` على قائمة الجوال المنسدلة موجود أصلًا من جولة سابقة (لا تكرار).
- **لم يُنفَّذ**: تدقيق تباين ألوان شامل، مراجعة ترتيب Tab لكل صفحة، aria-describedby لربط كل
  خطأ بحقله تحديدًا (اكتفيت بـ`role="alert"` كخطوة أولى فعّالة وأقل تدخّلًا في البنية).

### 7) Responsive (القسم 30)
راجعت المكوّنات الجديدة تحديدًا لخطر Overflow: `.checkout-grid`/`.browser-frame-body` أُضيف
لهما تعديل padding مخصَّص لـ≤480px. **لم يُختبَر بصريًا فعليًا على أي مقاس** — لا متصفح متاح
في هذه البيئة، ولن أدّعي خلاف ذلك. هذا استنتاج CSS منطقي (قواعد `clamp()`/`grid-template-columns`
المتجاوبة موجودة وصحيحة نظريًا) لا معاينة حقيقية.

### 8) توحيد "الخطة" — تدقيق نهائي
تحققت أن الملفات المعدَّلة هذه الجولة (Checkout/Payment الجديدة) تستخدم "الخطة" حصرًا، متسقة
مع الجولة السابقة — لا "باقة" جديدة تسرّبت.

### الملفات المعدَّلة هذه الجولة (17 ملفًا، صفر لمس لأي API/schema/منطق مُقفَل)
`src/app/motabaa/enroll/{EnrollForm,page}.tsx`، `src/app/motabaa/enroll/payment/
{PaymentClient,page}.tsx`، `src/app/parent/schedule/{ParentDashboardHeader,UpcomingSessions}.tsx`،
`src/app/page.tsx`، `src/components/Shell.tsx`، `src/app/globals.css`، + 10 ملفات لإضافة
`role="alert"` فقط (`RecommendationCard`, `PauseRequestForm`, `CohortOperationsForm`,
`GoalForm`, `AssessmentForm`, `SessionReportForm`, `PilotLoginSection`, `login/page`,
`PaymentClient`, `EnrollForm`).

### نتيجة npm run build / npm run lint (نهائي)
**كلاهما FAIL فعلي** — نفس القيد الشبكي المتكرر عبر كل جولة في هذا المشروع بالكامل (`403
Forbidden` على `npm install`، بيئة الحاوية هنا بلا وصول إنترنت مطلقًا). فحصت توازن الأقواس آليًا
عبر **كل** ملفات TS/TSX في المشروع (سليم)، وشغّلت `scripts/qa-plan-days.mjs` فعليًا (PASS)،
وتحققت بـ`diff` حرفي أن كل دالة منطقية حسّاسة (submit/confirm/دوال Supabase) لم تتغيّر حرفًا
واحدًا في أي ملف لمسته. **هذا ليس بديلًا عن Build حقيقي** — يبقى يحتاج تشغيلك على بيئة بوصول
شبكة فعلي (جهازك، المؤكَّد نجاحه سابقًا لهذا المشروع بالضبط).

### الحالة النهائية مقابل الطلب الأصلي (41 قسمًا)
**مُنفَّذ بعمق حقيقي**: P0 (Plan/Days/Sessions integrity + QA مُثبَت)، Security review مستهدف،
Arabic copy (Pilot/Focus Room/رسائل خام/Terms-Privacy/توحيد "الخطة")، SEO أساسي، Admin
operational essentials، Trust Metrics، Stepper، Checkout، Payment، Parent Dashboard uplift،
Product Showcase، Skip Link + role=alert.
**مُنفَّذ جزئيًا وموثَّق بصراحة**: Homepage storytelling (بالاستفادة من بنية موجودة بدل تكرار)،
Accessibility (نقطتان محدَّدتان، لا Audit شامل).
**لم يُنفَّذ، وأُوصي به كجولة تصميم منفصلة مخصَّصة**: إعادة تصميم Corporate عميقة بمستوى
Aramco/NEOM لكل صفحة (Typography/Composition/Motion على مستوى الوكالة)، Responsive testing
فعلي بمتصفح حقيقي على 8 مقاسات، Accessibility audit شامل (تباين/Tab order/aria-describedby)،
اختبار سيناريوهات A/B/C/D الأربعة E2E كاملة (يحتاج خادمًا حيًا).

**لا أدّعي "اكتمل 100%"** — القائمة أعلاه دقيقة ومقصودة كأساس صادق لأي قرار إطلاق.

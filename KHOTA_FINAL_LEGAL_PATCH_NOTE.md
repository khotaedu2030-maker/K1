# Final Legal Patch — تأكيد التنفيذ

## الملفات الخمسة المعدَّلة (لا شيء آخر — تحقّق `find` مباشر يؤكّد ذلك)
1. `src/lib/legal-profile.ts` — `lastLegalUpdate: "2026-09-15"`.
2. `src/app/refund-policy/page.tsx` — قسم "تعذّر تقديم الخدمة" استُبدِل بالصياغة الصريحة
   لمهلة الـ15 يومًا حرفيًا كما أعطيتها. أُزيلت أيضًا الفقرة التي كانت تحيل على `/contact`
   كقناة فعلية (كانت في قسم "كيف تقدّم طلب الاسترداد") واستُبدِلت بصياغة محايدة "عند تفعيلها".
3. `src/app/contact/page.tsx` — زر "إرسال" أصبح `disabled` فعليًا (مع `title` توضيحي)، حُذفت
   جملة "أخبرنا وسنعود إليك" الموحية بأن النموذج يعمل، وأُضيفت ملاحظة صريحة: "قناة الدعم
   الإلكترونية عبر هذا النموذج غير مفعَّلة بعد. سيتم تفعيلها قريبًا."
4. `src/app/complaints/page.tsx` — قسم "كيف تُقدَّم الشكوى": حُذف رابط `/contact` والجملة
   المرتبطة به، استُبدِل بـ"تُقدَّم طلبات الشكاوى والمقترحات عبر قناة الدعم الرسمية لخُطى عند
   تفعيلها." قسم "المعالجة": حُذفت "نتواصل معك" الفعلية، استُبدِلت بصياغة مشروطة "متى كانت
   قناة الدعم مفعَّلة."
5. `src/app/sitemap.ts` — روابط مطلقة الآن عبر `https://khota.sa` بدل المسارات النسبية، وحُذف
   التعليق القديم "لا نطاق إنتاج معروف بعد".

**تنظيف تبعي بسيط**: أزلت استيراد `Link` من `complaints/page.tsx` و`refund-policy/page.tsx`
بعد أن أصبح غير مستخدَم عقب حذف روابط `/contact` منهما — لا تغيير وظيفي، فقط تجنّب استيراد ميت.

## لم يُلمَس أي شيء آخر
تحقّق `diff` حرفي: منطق الفلترة في `PlansSelector.tsx` مطابق تمامًا. تحقّق مباشر: دالة
`confirm()` بمنطق الدفع لم تُلمَس. صفر تغيير على أي ملف API أو Supabase أو Middleware أو
Auth أو الأسعار. صفر `git push` أو `vercel deploy` — لا تتوفر هذه الأدوات أصلًا في هذه البيئة.

## نتيجة الاختبارات — بصراحة تامة كما طلبت
`npm install`: **فشل** — `403 Forbidden`، لا وصول شبكة في هذه البيئة (القيد نفسه الحاضر في كل
جولة من هذا المشروع منذ البداية، لم يتغيّر). نتيجة لذلك: `npx tsc --noEmit` يعمل لكن بلا حزم
حقيقية مثبَّتة (نفس ضجيج "Cannot find module" المعتاد لا يعكس أخطاء كود)، و`npm run build`
يفشل بـ`next: not found` مباشرة.

**لم أتمكَّن من تشغيل الأمرين بنجاح في "البيئة الأصلية التي تحتوي على node_modules"** كما
طلبت صراحةً — هذه البيئة (Sandbox) لا تملك وصولًا للشبكة إطلاقًا، وهذا قيد ثابت لم يتغيّر عبر
كل هذا المشروع. **لا أعتبر هذه النسخة جاهزة حتى تُشغِّل أنت `npx tsc --noEmit` و`npm run build`
فعليًا على جهازك أو بيئة CI حقيقية وتتأكّد من نجاحهما** — هذا بالضبط ما طلبته، ولن أدّعي خلاف
ذلك. التحقّق البديل الوحيد المتاح لي هنا: توازن الأقواس سليم عبر كل ملفات TS/TSX (فحص آلي)،
وفحصت تحديدًا الملفات الخمسة الممسوسة بحثًا عن implicit-any جديد — صفر نتيجة.

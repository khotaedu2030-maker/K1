# KHOTA Redesign — Acceptance Report (صادق، لا تسويق)

## Route-by-Route Proof Table

| Route | إعادة بناء JSX/Layout فعلية؟ | التغيير البنيوي الحقيقي |
|---|---|---|
| `/` | نعم (جولة سابقة) | Hero Full-bleed، Split-screen مزدوج، Stage Picker تفاعلي |
| `/start` | **نعم — هذه الجولة** | من `.narrow+.cards` متساويين → Split-screen بتراتبية حقيقية (CTA أساسي كبير، رابط ثانوي نصّي تحته) |
| `/login` | نعم (جولة سابقة) | Split-screen (نموذج + صورة)، المنطق لم يُلمَس |
| `/motabaa` | **نعم — هذه الجولة** | أُضيفت بعد الـHero: بيان غامق، رحلة Method، Split-screen مزدوج (طالب/والد)، Stage Picker، Brand Moment — بدل انتهائها بـ`.cards` |
| `/motabaa/plans` | **نعم — هذه الجولة** | من عمود واحد + شريط ملخّص سفلي → عمودين Desktop حقيقيين (تدفّق رئيسي + لوحة ملخّص Sticky تُبنى تدريجيًا)، Grade Tiles كبيرة بدل Chips صغيرة، Plan Tiles بعلامة "الأكثر اختيارًا" + نقاط تكرار بصرية، Group Tiles بشارات أيام دائرية. **المنطق مطابق حرفيًا (`diff` مؤكَّد)** |
| `/motabaa/enroll` | نعم (جولة سابقة، مؤكَّد كافٍ) | Checkout عمودين حقيقي (لوحة تفاصيل + نموذج)، `submit()` مطابق حرفيًا |
| `/motabaa/enroll/payment` | نعم (جولة سابقة) | نفس البنية عمودين، `confirm()` مطابق حرفيًا |
| `/parent/schedule` | **نعم — هذه الجولة** | من عمود واحد مكدَّس → App Layout عمودين حقيقي (1.6fr رئيسي: الجلسة القادمة + Timeline / 1fr جانبي: الملخص الأسبوعي + رحلة الطفل + رصيد التعويض). **كل استعلامات Supabase قبل `return` لم تُلمَس** |
| `/parent` | **نعم — هذه الجولة** | نفس النمط: بطاقة الإنجاز كـHero بالعمود الرئيسي + مهام حديثة، عمود جانبي لخطوة الأسبوع/المؤشر/التوصية/روابط سريعة — بدل `.dashboard-grid` + `.cards` المسطَّحة القديمة |
| `/about` | **نعم — هذه الجولة** | من Hero + شبكة "مبادئ" 4 خلايا → Hero + بيان غامق + سرد مرقَّم بصفوف (لا شبكة متطابقة) + Brand Moment |
| `/help` | **نعم — هذه الجولة** | من قائمة أسئلة مسطَّحة → Accordion تفاعلي حقيقي (`FaqAccordion.tsx`، مكوّن جديد بحالة توسيع/طي فعلية) + شريط تعريف غامق |
| `/contact` | **نعم — هذه الجولة** | من `.narrow`+نموذج بسيط → Split-screen (بيان + نموذج / صورة) |
| `/teachers` | **نعم — هذه الجولة** | من قائمة مراحل نصّية → Split-screen + صفوف مرقَّمة بأوصاف حقيقية بدل عناوين فقط |
| `/teach-with-khota` | **نعم — هذه الجولة** | من `.narrow`+نموذج → Split-screen (صورة + نموذج). **إصلاح إضافي**: كان يعرض "Focus Room" بالإنجليزي — أُصلح لـ"غرفة التركيز" |
| `/privacy`, `/terms` | لمسة خفيفة مقصودة | أُضيف تنقّل داخلي بين الأقسام (Anchor links) — لا Hero ضخم عمدًا (تعليمات صريحة بعدم الحاجة له لصفحات قانونية) |
| `/motabaa/how-it-works` | لا (خارج نطاق هذه الجولة) | لم تُفحَص بنيويًا هذه الجولة — تستخدم `.hero-image-frame` من جولة أقدم |
| `/motabaa/grades-1-3`, `/grades-4-6` | لا (يتيمتان مؤكَّد) | لا رابط يشير إليهما من أي تنقّل حالي (تحقّق `grep` سابق) — لم تُعادا بناء لعدم وجود مسار مستخدم يصل إليهما |
| `/teacher/*`, `/admin/*` | **لا — صريح** | لم تُعَد بناؤها بنيويًا هذه الجولة. أُصلح فيها Bug حقيقي (قيم Enum خام — راجع القسم التالي) لكن التركيبة العامة (Dashboard grids, tables) لم تُعَد تصميمها. هذه أدوات داخلية تشغيلية، أولوية أقل من الصفحات العامة/الأسرية حسب توجيهك السابق، لكن أُصرِّح بذلك بدل الادّعاء |
| `/parent/children`, `/messages`, `/recommendations`, `/subscriptions`, `/reports`, `/reports/weekly` | **لا** | لم تُفحَص بنيويًا هذه الجولة (فيما عدا إصلاح تواريخ خامة في `/subscriptions` من جولة سابقة). تستخدم نظام التصميم المشترك (نفس الطباعة/الألوان) لكن لم تُعَد هيكلتها |
| `/parent/payments` | لا حاجة | Placeholder صادق ("قيد التطوير") — لا محتوى ليُعاد تصميمه |
| `/student/*` | **لم يُلمَس إطلاقًا** | لا تغيير، لا وصول جديد فُتح، القيود الأمنية المعروفة (JWT مشترك) كما هي — موثَّقة كـP1 سابقًا |

## Raw Value Bugs — اكتُشفت وأُصلحت (4 حالات حقيقية)
1. `/teacher` (لوحة المعلم): `{s.status}` خام (scheduled/completed) → `sessionStatusLabelAr()`.
2. `GoalStatusButtons.tsx`: `{status}` خام (achieved/partially_achieved/carried_forward) → خريطة عربية جديدة.
3. `/admin/groups`: `{c.activeCount} / {c.capacity}` بلا تسمية تحت اسم المجموعة → "٢ من ٣ مقعدًا".
4. `/parent/subscriptions`: تواريخ ISO خامة → منسّق عربي.
5. **جديد هذه الجولة**: `/parent/page.tsx` — `weeklyGoal.status` كان يسقط لعرض القيمة الخام لأي حالة غير `achieved`/`active` (مثل `partially_achieved`) → أُضيفت خريطة `goalStatusLabel` كاملة.

**لم ألقَ `days_per_week` خامة بأي مكان** رغم بحث `grep` شامل متكرر عبر جولتين — لا أستطيع تأكيد كونها مصدر الشكوى الأصلية بيقين تام.

## المنطق الوظيفي — إثبات لا وعد
- `submit()` في `EnrollForm.tsx`: مطابق حرفيًا (diff فارغ، جولات متعددة).
- `confirm()` في `PaymentClient.tsx`: مطابق حرفيًا.
- منطق الفلترة الكامل في `PlansSelector.tsx` (`gradeBand`, `availablePlans`, `matchingCohorts`, `selectGrade`): مطابق حرفيًا هذه الجولة تحديدًا (diff فارغ) رغم إعادة بناء JSX بالكامل حوله.
- استعلامات Supabase في `/parent/schedule` و`/parent/page.tsx`: لم تُلمَس — فقط الـJSX المُعاد بعد `return (` تغيّر.
- إضافتان محدودتان لحقول SELECT (`days_per_week`, `sessions_per_month` في `/motabaa/plans/page.tsx`): عرض فقط، لا تُستخدَم في أي فلترة أو شرط.

## Build/Lint
**لم يُشغَّل بنجاح هنا** — نفس القيد الشبكي (`403`) في كل جولة من هذا المشروع. توازن الأقواس سليم عبر
كل ملفات TS/TSX (فحص آلي شامل)، `scripts/qa-plan-days.mjs` نُفِّذ فعليًا (PASS).

## الحكم النهائي (بصدق، لا "الموقع أصبح احترافيًا 100%")
**تم فعليًا**: إعادة بناء بنيوية حقيقية لـ11 مسارًا عامًا/أسريًا (الأهم حسب توجيهك)، إصلاح 5 أخطاء
قيمة-خام حقيقية، إثبات صريح أن كل منطق حسّاس لم يتأثر.
**لم يتم**: إعادة بناء `/teacher/*`، `/admin/*`، وبقية مسارات `/parent/*` الثانوية بنيويًا — تبقى
تعمل بنظام التصميم المشترك لكن دون التركيبة الجديدة. `/motabaa/how-it-works` لم تُفحَص. هذا التمييز
مقصود ومُصرَّح به، لا إغفال.

---

## Addendum — Continuation

### أُنجز هذه الجولة
- **`/motabaa/how-it-works`**: كان لا يزال يستخدم `ImageFeature` (المساحة الفارغة القديمة من
  قبل إضافة الصور الحقيقية!) بدل صورة فعلية، وHero عام `.hero`/`.grid` بدل نظام Split-screen
  الجديد — أُصلح كليًا (صورة `khota-secondary.webp` حقيقية + Split-screen).
- **`/parent/children`**: bug قيمة خام حقيقي جديد — `الصف {c.grade}` كان يعرض رقمًا خامًا
  ("الصف 5") بدل `getGradeLabelArabic()` المستخدَمة بكل مكان آخر بالمشروع. أُصلح + رُفعت بطاقة
  كل طفل من `.session-row` عام إلى `.dashcard` بمحاذاة أوضح.
- **سويبة أخيرة شاملة** لـ`{x.status}`/`{x.grade}` خام عبر `/parent/*` بالكامل وTeacher/Admin —
  لم تُوجَد حالات إضافية غير ما أُصلح.

### Route-by-Route — تحديث
| Route | الحالة |
|---|---|
| `/motabaa/how-it-works` | **أُصلحت هذه الجولة** (كانت "لم تُفحَص" في التقرير السابق) |
| `/parent/children` | **أُصلح bug قيمة خام حقيقي + رفع بطاقة** |
| `/parent/recommendations`, `/subscriptions`, `/reports`, `/reports/weekly`, `/messages` | **لا تزال غير مُعاد بناؤها بنيويًا** — فُحصت لقيم خام (نظيفة) لكن التركيبة العامة لم تتغيّر |
| `/teacher/*`, `/admin/*` | **لا تزال غير مُعاد بناؤها بنيويًا** — فُحصت لقيم خام (نظيفة، الإصلاح السابق كافٍ) |

### Build
**FAIL فعلي لنفس السبب المتكرر** (403، بيئة بلا شبكة). توازن الأقواس سليم، QA script نُفِّذ
فعليًا (PASS).

### الحكم النهائي المُحدَّث
تمت تغطية **كل** المسارات العامة/التسويقية المطلوبة صراحة (`/start`, `/motabaa`,
`/motabaa/how-it-works`, `/motabaa/plans`, `/about`, `/help`, `/contact`, `/teachers`,
`/teach-with-khota`) + الصفحتان الأهم بالمنتج (`/parent`, `/parent/schedule`) بإعادة بناء
بنيوية حقيقية موثَّقة. المتبقي صراحة: بقية `/parent/*` الثانوية، و`/teacher/*`، و`/admin/*` —
نظيفة من القيم الخام لكن غير مُعاد تصميمها هيكليًا.

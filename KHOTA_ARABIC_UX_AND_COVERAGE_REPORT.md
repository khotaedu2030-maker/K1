# KHOTA — Arabic-First UX + Plan/Cohort Coverage Report

## لا أقول COMPLETE
هذه جولة ضخمة (19 صفحة + مكوّنات مشتركة). راجعت وصلحت **حقيقيًا** ما أمكن ضمن الوقت المتاح،
وأُصرِّح بدقة في القسم الأخير بما لم يُراجَع سطرًا بسطر بعد — لا ادّعاء عام بأن "كل نص رُوجِع".

---

## الجزء الوظيفي: Plan/Cohort Coverage

### الحالة الحقيقية (تحقّقت، لم أفترض)
راجعت ملفَي seed معًا (`seed_pilot_wave1.sql` + `seed_pilot_wave1_missing_cohorts.sql` من جولة
سابقة) باستخلاص كل صف `insert` فعليًا — **الاثنتا عشرة تركيبة (plan × grade_band) مغطّاة
بالكامل بين الملفين**. المشكلة أن **ملف الإصلاح الثاني لم يُشغَّل بعد على Supabase** — هذا سبب
استمرار ظهور "لا توجد مجموعة متاحة" لبعض الخطط.

### التسليم الجديد هذه الجولة
1. **`supabase/verification/plan_cohort_coverage_check.sql`** — استعلام قراءة فقط (بلا أي
   كتابة)، يعرض الاثنتي عشرة تركيبة مقابل ما هو موجود فعليًا في `cohorts`، مع عمود
   `coverage_status` (✅/❌) لكل واحدة. **شغّله بنفسك في Supabase SQL Editor للتأكد الفعلي** —
   لا أملك اتصالًا حيًا بقاعدتك هنا لأشغّله بنفسي.
2. **`supabase/seed_plan_names_arabic_fix.sql`** (جديد، منفصل، **غير مُشغَّل تلقائيًا**) —
   اكتشفت أثناء المراجعة أن `plans.name` لخطط Focus Room مخزَّنة فعليًا في القاعدة كـ**"Focus
   Room — يومان أسبوعيًا"** إلخ — إنجليزي داخل اسم الخطة نفسه، بيانات لا كود. الإصلاح: نفس
   أسماء خُطى متابعة تمامًا (الانطلاقة/الأساسية/المكثفة) — نوع الخدمة يظهر بشكل منفصل.
3. **كلا الملفين يحتاجان تشغيلك اليدوي** — لم أُشغِّل أيهما.

### عرض المجموعة النظيف (جديد هذه الجولة)
أضفت `formatCohortDisplayName()` في `src/lib/plan-display.ts` — يحوّل "Focus Room 10-12 —
المجموعة B" إلى **"المجموعة (ب)"** فقط (حروف عربية، بلا grade_band، بلا اسم برنامج تقني).
طُبِّق في: بطاقات اختيار المجموعة ولوحة الملخّص في `/motabaa/plans`، ملخّص `/motabaa/enroll`
و`/payment`. `grade_band`/`product` تبقيان داخليتين للفلترة فقط، لا تظهران للمستخدم.

## المنطق الوظيفي — إثبات لا وعد
أعدت تشغيل `diff` على منطق الفلترة الكامل في `PlansSelector.tsx` بعد كل هذه التعديلات
(النصية والبيانات) — **مطابق حرفيًا**. لم أُنشئ أي migration جديدة على `schema.sql`. القيد
`enforce_cohort_days_match_plan` لم يُلمَس. الأسعار/عدد الجلسات/منطق الدفع/التسجيل لم تتغيّر.

---

## الجزء اللغوي: Arabic-First UX Writing

### 1) الصفحات التي رُوجِعت فعليًا (تعديل نصي حقيقي)
`/motabaa/plans` (مراجعة عميقة — العنوان الأهم في الطلب) • `/motabaa/enroll` + `/payment`
(تسميات الملخّص) • `/parent/schedule` (بطاقة "تقدّم الطالب"، ترويسة الترحيب) • `/login` •
`/start` • `/student/tasks`، `/student/progress` (إزالة عامية) • `StagePicker.tsx` (مكوّن
مشترك تظهر نصوصه في `/` و`/motabaa`).

### 2) أهم المصطلحات التي وُحِّدت
- **"رحلة الطفل/رحلتك" → "تقدّم الطالب" / "متابعة طفلك"** — حسب اقتراحك الصريح ("بدل مفهوم
  journey... تقدّم الطالب"). طُبِّق في 4 مواضع (`ChildJourney`, `ParentDashboardHeader`,
  `/start`, `/login`).
- **"ملخص اختيارك" → "اختيارك"** — مطابق حرفيًا لمثالك.
- **"الصف" → "الصف الدراسي"، "الأيام والوقت" (سطر مدمج) → "الأيام" + "الموعد" (سطران
  منفصلان)، "السعر" → "الاشتراك"** في لوحة ملخّص `/motabaa/plans` — مطابق حرفيًا لمثالك.
- **عناوين الخطوات**: "الخطة" → "اختر الخطة"، "المجموعة" → "اختر المجموعة".
- **عامية → فصحى بسيطة**: "ما فيه مهام مسجّلة بعد" → "لا توجد مهام مسجّلة بعد"، "لسه ما فيه...
  تشوف" → "لا توجد بيانات كافية بعد — سيظهر تقدّمك هنا قريبًا."
- **"مساحة تركيز حقيقية" → "تركيز فعلي"** (أبسط، أقل صياغة تسويقية).

### 3) أمثلة Before → After (حقيقية من الكود، لا افتراضية)
| قبل | بعد |
|---|---|
| ملخص اختيارك | اختيارك |
| الأيام والوقت: الأحد+الثلاثاء • 16:30 | الأيام: الأحد، الثلاثاء / الموعد: 16:30 (سطران) |
| السعر: 399 ر.س / شهريًا | الاشتراك: 399 ر.س شهريًا |
| هذا ملخص رحلة طفلك مع خُطى. | هذا ملخص متابعة طفلك مع خُطى. |
| ابدأ رحلة طفلك مع خُطى. | ابدأ متابعة طفلك مع خُطى. |
| ...للوصول إلى حسابك ومتابعة رحلة طفلك. | ...للوصول إلى حسابك ومتابعة تقدّم طفلك. |
| Focus Room 10-12 — المجموعة B (خام) | المجموعة (ب) |
| Focus Room — 3 أيام أسبوعيًا (اسم خطة في القاعدة) | الأساسية (بعد تشغيل seed_plan_names_arabic_fix.sql) |
| ما فيه مهام مسجّلة بعد. | لا توجد مهام مسجّلة بعد. |

### 4) نص أُبقي بالإنجليزية، ولماذا
- **"KHOTA"** في الشعار/الهوية البصرية فقط (Logo.tsx) — حسب توجيهك صراحة أنها تبقى ضمن الهوية.
- **أسماء دوال/متغيرات الكود** (`formatCohortDisplayName` إلخ) — إنجليزية كما هو متعارف عليه
  في الكود نفسه، لا تظهر للمستخدم إطلاقًا.
- لا يوجد أي نص إنجليزي آخر متبقٍ ظاهر للمستخدم في المسارات التي راجعتها (تحقّق `grep` شامل
  لكلمة "Focus Room" تحديدًا — صفر نتيجة في JSX المعروض).

### 5) لم يُراجَع سطرًا بسطر بعد (صريح، لا إغفال)
`/motabaa` (النص الأساسي — بُنيته سابقًا، لم أُعِد قراءته بمعيار "هل يبدو مترجمًا" هذه الجولة
تحديدًا) • `/motabaa/how-it-works` • `/about`, `/help`, `/contact`, `/teachers`,
`/teach-with-khota` (بُنيت بصريًا في جولات سابقة، لم تُراجَع لغويًا بهذا المعيار الدقيق هذه
الجولة) • `/parent/children`, `/messages`, `/subscriptions`, `/reports`, `/reports/weekly` •
Homepage (`/`) بالكامل (Hero/Method/Split-screens) — راجعتها لغويًا في جولة سابقة (تحرير نص
"المشكلة")، لم أُعِد قراءتها بالكامل هذه الجولة تحديدًا.

## Build/Lint
**لم يُشغَّل بنجاح هنا** — نفس القيد الشبكي (`403`) المتكرر في كل جولة. توازن الأقواس سليم عبر
كل ملفات TS/TSX، `scripts/qa-plan-days.mjs` نُفِّذ فعليًا (PASS).

## ما يحتاج تشغيله منك في Supabase (الخطوتان الوحيدتان المتبقيتان لإغلاق المشكلة فعليًا)
```
supabase/seed_pilot_wave1_missing_cohorts.sql   -- إن لم يُشغَّل بعد
supabase/seed_plan_names_arabic_fix.sql          -- جديد هذه الجولة
```
ثم شغّل `supabase/verification/plan_cohort_coverage_check.sql` للتأكد المباشر من 12/12 ✅.

---

## Addendum — Continuation (باقي الصفحات رُوجعت فعليًا)

استخرجت كل نص مرئي فعليًا (سطرًا بسطر عبر `grep`، لا قراءة سريعة) من: `/motabaa`, `/about`,
`/help`, `/contact`, `/teachers`, `/teach-with-khota`, كل صفحات `/parent/*` المتبقية
(children/messages/recommendations/subscriptions/reports)، والصفحة الرئيسية كاملة.

### النتيجة الصادقة
**معظمها كان بالفعل عربيًا طبيعيًا** — بُنيت بعناية في جولات سابقة. لم أُعِد كتابة كل شيء
تعسّفًا؛ عدّلت فقط ما فشل فعليًا في اختبار "هل يقولها منتج سعودي محترف؟":

- **`/parent/messages`**: "قناة تواصل مباشرة" → "تواصل مباشر" (حذف "قناة" التقنية).
- **الصفحة الرئيسية**: "رحلة واحدة، بمنهجية ثابتة." → "أسلوب واحد ثابت، في كل جلسة." و"تبدأ
  رحلة خُطى" → "تبدأ متابعة خُطى" — نفس نمط استبدال "رحلة" المطبَّق سابقًا، للاتساق الكامل عبر
  الموقع (لم أكن قد راجعت الصفحة الرئيسية بالكامل بهذا المعيار في الدفعة الأولى).

### تحقّق نهائي شامل
بحثت آليًا (`grep`) عن أي بقايا "رحلة" (بمعنى journey) في **كل** ملفات JSX — الأمثلة المتبقية
كلها "مرحلة" (stage/grade) غير ذات صلة، مؤكَّد لا مطابقة زائفة. **صفر استخدام متبقٍ لإطار
"رحلة" التسويقي المترجم في أي نص مرئي للمستخدم عبر الموقع بالكامل.**

### الصفحات الآن مُراجَعة فعليًا (100% من النطاق المطلوب)
كل الـ19 صفحة/مكوّن المذكورة في "FULL SITE SCOPE" استُخرِج نصّها ورُوجِع فعليًا هذه الجولة —
لا صفحة متبقية بدون مراجعة نصية على الأقل مرة واحدة.

## Build (نهائي)
**FAIL فعلي لنفس السبب المتكرر** (403، بيئة بلا شبكة). توازن الأقواس سليم، QA script نُفِّذ
فعليًا (PASS) بعد كل الدفعات.

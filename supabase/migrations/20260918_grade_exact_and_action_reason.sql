-- (1) صف دقيق اختياري لكل مجموعة — grade_band وحده (نطاق واسع) لا يكفي لتمييز مجموعات
-- بنفس النطاق لكن بصفوف مختلفة فعليًا (تحققت من seed_pilot_wave1.sql مباشرة: مجموعة واحدة
-- بعنوان "4-6" تخدم الصفوف 4 و5 و6 معًا حاليًا بلا تمييز). عمود جديد اختياري (nullable) —
-- كل الصفوف الحالية تبقى صالحة بلا أي تعديل عليها، ويستمر عمل grade_band كما هو للتوافق مع
-- منطق التسجيل الحالي (المطابقة تبقى بالـband ما لم يُضبَط grade تحديدًا لاحقًا لمجموعة معيَّنة).
alter table cohorts add column if not exists grade smallint check (grade between 1 and 12);

-- (2) سبب نصي اختياري لأي عملية إدارية حساسة (تعيين معلم بديل، منح/إلغاء رصيد تعويض يدويًا،
-- إلخ) — إضافة صرفة لا تمسّ أي سجل admin_actions موجود، ولا تُنشئ جدول تدقيق جديد (نفس الجدول
-- الحالي فقط، عمود إضافي واحد).
alter table admin_actions add column if not exists reason text;

# KHOTA — Plan Flow Final Fix Report

## 1) الملفات المعدَّلة
**جديد**: `src/lib/plan-display.ts` (مصدر مركزي لأسماء الأيام/ترتيبها/صياغة الأعداد)،
`supabase/seed_pilot_wave1_missing_cohorts.sql` (منفصل، **غير مُشغَّل تلقائيًا**).
**معدَّل**: `src/app/motabaa/plans/{PlansSelector,page}.tsx`، `src/app/motabaa/enroll/
{EnrollForm,page}.tsx`، `src/app/motabaa/enroll/payment/{PaymentClient,page}.tsx`،
`src/app/parent/schedule/page.tsx`، `src/app/admin/groups/page.tsx`، `scripts/qa-plan-days.mjs`.

## 2) Root Cause الحقيقي
**فجوة بيانات، وليست خطأ كودًا.** تتبعت المسار كاملًا (RPC → query → فلترة الواجهة) ووجدته
سليمًا بنيويًا. المشكلة الفعلية: `seed_pilot_wave1.sql` الأصلي زرع 7 مجموعات فقط، لم تغطِ كل
تركيبة (plan × grade_band):
- `khota-4` (المكثفة، 4 أيام): **لا مجموعة له في band 1-3** (موجود فقط في 4-6).
- `focus-2` (Focus Room يومان): **لا مجموعة له إطلاقًا** (لا 7-9 ولا 10-12).
- `focus-4` (Focus Room 4 أيام): **لا مجموعة له إطلاقًا** (لا 7-9 ولا 10-12).

`focus-3` (3 أيام) هو الخطة الوحيدة التي تملك مجموعة في **كل** مراحل Focus Room — هذا يفسّر
تمامًا لماذا بدت خطة 3 أيام "تعمل دائمًا" بينما 2 و4 "لا تعملان" عند اختبار صفوف 7-9/10-12.
ولخطة motabaa (1-6): 2 و3 أيام تعملان لكلا الفرعين 1-3 و4-6، والمكثفة (4 أيام) ناقصة لـ1-3
تحديدًا. أثبتّ هذا بسكربت مطابقة فعلي (Part 3 في `qa-plan-days.mjs`)، لا بافتراض.

## 3) نتيجة اختبار خطة يومين (2-day)
**Band 1-3 و4-6 (motabaa)**: تعمل فعليًا بالبيانات الحالية — مجموعة `khota-2` موجودة لكلا
الفرعين. **Band 7-9/10-12 (Focus Room)**: لا تعمل حاليًا (`focus-2` بلا مجموعة) — **يحتاج
تشغيل `seed_pilot_wave1_missing_cohorts.sql`**. هذا تتبّع منطقي مؤكَّد بالبيانات، **لم أُشغِّل
خادمًا حيًا لأضغط الأزرار فعليًا** — لا بيئة متصفح متاحة هنا.

## 4) نتيجة اختبار خطة 3 أيام (3-day)
تعمل فعليًا في **كل** المراحل الأربع (1-3, 4-6, 7-9, 10-12) بالبيانات الحالية — لا تغيير مطلوب،
ولم أُدخِل أي تعديل قد يكسرها (تحقّق `diff` حرفي مؤكَّد أن منطق الفلترة نفسه لم يتغيّر إطلاقًا).

## 5) نتيجة اختبار خطة 4 أيام (4-day)
**Band 4-6 (motabaa)**: تعمل فعليًا بالبيانات الحالية. **Band 1-3 (motabaa) وFocus Room بالكامل
(7-9, 10-12)**: لا تعمل حاليًا — **تحتاج تشغيل ملف الإصلاح**. نفس التحفّظ: تتبّع منطقي، لا
اختبار حي.

## 6) هل احتجنا SQL؟ نعم
`supabase/seed_pilot_wave1_missing_cohorts.sql` — **لم أُشغِّله**. يضيف 5 مجموعات فقط
(بالتفصيل في القسم التالي)، يمرّ عبر قيد `enforce_cohort_days_match_plan` الموجود مسبقًا (لم
يُلمَس ولم يُتحايَل عليه)، Idempotent (`where not exists`) بنفس نمط الملف الأصلي.

## 7) ما يحتاج تشغيله منك في Supabase
```
supabase/seed_pilot_wave1_missing_cohorts.sql
```
يضيف بالضبط:
- خُطى متابعة 1-3 — المجموعة C (khota-4، 4 أيام: أحد-اثنين-ثلاثاء-أربعاء، 3 مقاعد)
- Focus Room 7-9 — المجموعة B (focus-2، يومان: أحد-ثلاثاء، 5 مقاعد)
- Focus Room 10-12 — المجموعة B (focus-2، يومان: أحد-ثلاثاء، 5 مقاعد)
- Focus Room 7-9 — المجموعة C (focus-4، 4 أيام، 5 مقاعد)
- Focus Room 10-12 — المجموعة C (focus-4، 4 أيام، 5 مقاعد)

بعد التشغيل، **كل** تركيبة (plan × grade_band) الاثنتي عشرة الممكنة تملك مجموعة واحدة على
الأقل. لا حاجة لأي migration على `schema.sql` نفسه — القيد الموجود يكفي ويُطبَّق تلقائيًا على
الصفوف الجديدة.

---

## إصلاحات إضافية (Additional Final UX Fixes) — منفَّذة

**1) ملخّص موحَّد**: أنشأت `src/lib/plan-display.ts` — استبدل 3 خرائط أيام محلية مكرَّرة
(كانت بكل من `PlansSelector`، `enroll/page`، `payment/page`) بمصدر واحد. أضفت حقل **"عدد
الجلسات"** المفقود سابقًا إلى ملخّص Enrollment وPayment، وأضفت **بطاقة "الخطة الحالية"** جديدة
كليًا للوحة ولي الأمر (لم تكن موجودة قبل هذه الجولة) — الآن الحقول الست (البرنامج/الخطة/الأيام/
عدد الجلسات/المجموعة/الوقت/السعر) تظهر بنفس التسميات في الأربع نقاط.

**2) No-Availability State**: بدّلت الرسالة العامة القديمة برسالة واضحة "لا توجد مجموعة متاحة
لهذه الخطة حاليًا." + زر فعلي "اختر خطة أخرى" يعيد المستخدم لخطوة الخطة (Anchor + إعادة تعيين
الاختيار) — لا Empty UI مبهم.

**3) الحفاظ على الاختيار**: أضفت مزامنة state↔query params (`?grade=&plan=&cohort=`) في
`PlansSelector` — **بلا أي client storage** كما طُلب صراحة. إن رجع المستخدم من صفحة التسجيل
بزر الرجوع، تُستعاد نفس اختياراته من الرابط نفسه.

**4) ترتيب الأيام العربي**: `formatDaysList()` يرتّب دائمًا حسب `WEEKDAY_NAMES_AR` الثابت
(0=الأحد..6=السبت) بصرف النظر عن ترتيب التخزين في القاعدة أو أي locale.

**5) صياغة الأعداد**: `formatDayCount`/`formatSessionCount`/`formatSeatCount` تطبّق القواعد
النحوية العربية الصحيحة (مفرد/مثنى/جمع قليل 3-10/جمع كثرة 11+) — لا "2 أيام" ولا "2 جلسات" بعد
الآن، بالضبط كما طُلب.

**6) Regression عبر اللوحة**: بطاقة "الخطة الحالية" الجديدة في `/parent/schedule` تسحب نفس
الأيام (`cohorts.days_of_week`) ونفس اسم الخطة من نفس مصدر البيانات المستخدَم في Enrollment/
Payment — لا تكرار منطق، نفس الدالة المركزية.

**7) القيم الخام**: بحثت فعليًا (`grep`) عبر رحلة الخطط/التسجيل/الدفع بالكامل — نظيفة. راجعت
أيضًا كل خرائط الحالة الأخرى في `/parent/*` (اشتراكات، أهداف) وتأكدت أنها **كاملة** (تغطي كل
قيمة enum ممكنة من قيود `schema.sql` نفسها) فلن تسقط لعرض قيمة خام أبدًا.

## المنطق الوظيفي — إثبات نهائي
`submit()` في `EnrollForm.tsx` و`confirm()` في `PaymentClient.tsx` ومنطق الفلترة الكامل في
`PlansSelector.tsx` (`gradeBand`, `availablePlans`, `matchingCohorts`, `selectGrade`): **مطابقة
حرفيًا** لما كانت عليه قبل بداية هذه الجولة — تحقّق `diff` فعلي مُعاد بعد كل دفعة تعديلات، لا
مرة واحدة فقط. القيد `enforce_cohort_days_match_plan` لم يُلمَس ولم يُعدَّل.

## Build/Lint
**لم يُشغَّل بنجاح هنا** — نفس القيد الشبكي (`403`) في كل جولة من هذا المشروع بلا استثناء.
توازن الأقواس سليم عبر **كل** ملفات TS/TSX (فحص آلي شامل بعد كل دفعة). `scripts/qa-plan-days.mjs`
**نُفِّذ فعليًا** (PASS، مُخرَجات حقيقية موثَّقة أعلاه) — Part 3 الجديد يوثّق بوضوح أنه تتبّع
منطقي لا اختبار حي، ولا يدّعي خلاف ذلك.

## لا أقول COMPLETE
حسب طلبك الصريح: **لم أختبر السيناريوهات الثلاثة على خادم حي فعليًا** — لا بيئة متصفح/تشغيل
متاحة هنا. ما قدَّمته: إثبات منطقي دقيق (سكربت + تتبّع بيانات فعلي، لا افتراض) لسبب المشكلة
ولما سيحدث بعد تشغيل ملف الإصلاح، وكل الإصلاحات البرمجية المطلوبة منفَّذة ومُتحقَّق من توازنها
وسلامة منطقها. **الخطوة الوحيدة المتبقية لإغلاق المشكلة فعليًا هي تشغيلك لملف الـSQL في
Supabase**، ثم تأكيدك أن الثلاث خطط تعمل حيًا.

# KHOTA V30 — Creative Correction (Breaking Repetition)

## What changed
Homepage scenes 06 (ولي الأمر) و07 (المعلمون) كانتا تستخدمان نفس مكوّن `EditorialSplit`
حرفيًا (60/40 و40/60 معكوس) — نفس التركيبة، صور مختلفة فقط. أُعيد بناء الاثنتين بتركيبتين
مختلفتين جذريًا:

- **Scene 06 (ولي الأمر)** → `.layered-reveal` جديد: نص كبير مهيمن بعرض كامل أعلى القسم، ثم
  صورة **أصغر** (58% من العرض فقط) معلَّقة بشكل غير متماثل لجهة واحدة تحته — ليست نصف الشاشة،
  عنصر داعم لا مكافئ للنص.
- **Scene 07 (المعلمون)** → `.quiet-cinematic` جديد: صورة تكاد تملأ الشاشة (58vh) بنص ضئيل جدًا
  في الزاوية فقط — تباين حجم جذري عن باقي المشاهد (أصغر نص، أكبر صورة نسبيًا من أي قسم آخر).

**Scene 05 (الجلسة) بقيت `EditorialSplit`** — الاستخدام التقليدي الوحيد المتبقي في الصفحة،
كما سُمح صراحة ("at most ONE conventional split").

## Final scene rhythm (10 مشاهد، صفر تكرار تركيبي)
Hero سينمائي كامل الشاشة → بيان طباعي بلا صورة → تسلسل طباعي بخط رفيع → شبكة صور 2×2 → **Split
تقليدي واحد فقط** (الجلسة) → كشف طبقي غير متماثل (ولي الأمر) → لحظة سينمائية هادئة بمقياس مختلف
(المعلمون) → بطاقات وظيفية (الخطط، مسموحة) → شريط نصّي (الثقة) → إغلاق سينمائي.

## Files modified
`src/app/globals.css` (إضافة `.layered-reveal` و`.quiet-cinematic` فقط، لا حذف) •
`src/app/page.tsx` (إعادة بناء Scene 06 و07 فقط، بقية الصفحة لم تُلمَس).

## Preserved from V29
لم تُلمَس: Header behavior وإصلاح الـposition bug، MobileNav (scroll lock/transition/keyboard)،
`--container-max:1360px`، `prefers-reduced-motion`، `/motabaa` و`/about` (لم يُذكَرا صراحة
هذه الجولة — التركيز كان "homepage" تحديدًا كما نص الطلب).

## Verification
`diff` حرفي على منطق `PlansSelector.tsx` — **مطابق تمامًا**. توازن أقواس سليم. صفر مرجع صورة
معلَّق. تحقّق `find`: **ملفان فقط** انلمسا هذه الجولة.

## Tests
`npm install`: **FAIL** (403، نفس القيد الشبكي). `npx tsc --noEmit`: نُفِّذ فعليًا (3301 سطر،
غالبيتها غياب node_modules، لا خطأ كود جديد). `npm run build`: **FAIL** (نتيجة مباشرة لفشل
التثبيت). **لا معاينة بصرية حية ولا Screenshots** — لا بيئة تشغيل متاحة هنا، كما في كل جولة
سابقة من هذا المشروع.

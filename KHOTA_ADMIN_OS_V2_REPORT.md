# KHOTA_ADMIN_OS_V2_REPORT

## صراحة أولى — النطاق الحقيقي المُنجَز مقابل الطلب
هذا طلب V2 هائل (24 بندًا). أعطيت الأولوية للأجزاء الأعلى قيمة/أمانًا وقابلة للإنجاز الحقيقي
بجلسة واحدة، وقرَّرت بوعي **عدم** بناء نظام الـDrawers الكامل (البنود 7-10) لأنه سطح واجهة ضخم
كان سيُبنى على عجل تحت ضغط وقت حقيقي — بالضبط النوع من المخاطرة التي طلبتَ تجنبها صراحة
("Do not call a feature complete if it is only UI"). موثَّق كأكبر عنصر مؤجَّل أدناه.

## 1) الملفات المتأثرة (9)
`admin/makeup-credits/{page,MakeupCreditActions}.tsx` (جديد/معدَّل) •
`admin/sessions/{page,SubstituteTeacherControl}.tsx` (جديد/معدَّل) •
`admin/teachers/page.tsx` (لُفَّ بـAdminShell، منطق الـScorecard الحالي **لم يُمَسّ حرفًا واحدًا**) •
`api/admin/{makeup-credits,sessions}/route.ts` (جديد) • `schema.sql` (إضافي فقط).

## 2) Migrations (لم تُطبَّق تلقائيًا)
`20260918_grade_exact_and_action_reason.sql` — عمودان إضافيان فقط:
- `cohorts.grade smallint nullable` (check 1-12)
- `admin_actions.reason text nullable`
كلاهما `alter table add column if not exists` — لا يُبطِل أي صف حالي، لا حذف، لا إعادة تسمية.

## 3) هل تطلَّب دعم الصف الدقيق تعديل schema؟ نعم — تحقّقتُ فعليًا
قرأت `seed_pilot_wave1.sql` مباشرة: مجموعة واحدة بعنوان "4-6" تخدم **الصفوف 4 و5 و6 معًا** بلا
أي تمييز — `grade_band` وحده لا يكفي فعليًا، وتأكّدت أن لا حقل آخر (title، slot_code) يُشفِّر
الصف الدقيق بشكل موثوق. لذلك أضفت `cohorts.grade` (nullable) — **إضافي بحت**، `grade_band`
يبقى كما هو للتوافق مع منطق التسجيل الحالي (`resolveGradeBand`) بلا أي تغيير.

## 4) كيف تُدعَم مجموعات متعددة لنفس الصف؟
بنيويًا مدعومة أصلًا (لا قيد يمنع أكثر من صف `cohorts` بنفس `grade_band`/`grade`) — لم يكن
يحتاج كودًا إضافيًا، فقط العمود الجديد لتمييزها دقيقًا عند الحاجة لاحقًا. **لم أبنِ واجهة
إنشاء مجموعة جديدة في هذه الجولة** (البند 14) — مؤجَّل، موضَّح أدناه.

## 5) كيف يدير معلم واحد مجموعات متعددة؟
مدعوم أصلًا بالـschema (`cohorts.teacher_id` بلا قيد تفرُّد) — لم يحتج أي تعديل.

## 6) كيف يعمل معلم الجلسة البديل؟
`POST /api/admin/sessions`: يُعدِّل `sessions.teacher_id` **لجلسة واحدة محدَّدة فقط** — لا يمسّ
`cohorts.teacher_id` (المعلم الأساسي) إطلاقًا. يرفض: جلسات غير `scheduled`، جلسات ماضية، معلم
غير نشط. الواجهة تُظهر "بديل (الأساسي: X)" عند الاختلاف. تأكيد + سبب إلزاميان، يُسجَّلان في
`admin_actions.reason` مع القديم/الجديد.

## 7) هل نُفِّذ كشف التعارض؟ نعم، على بيانات موثوقة فعليًا
استخدمت `sessions.starts_at/ends_at` (بيانات دقيقة فعلية لهذه الجلسة بالذات) — **وليس**
`teacher_availability` (تفضيلات أسبوعية عامة، ليست تعارضًا زمنيًا مؤكَّدًا). يرفض 409 لو
للمعلم البديل جلسة أخرى `scheduled` متداخلة زمنيًا.

## 8) تعديل جوال ولي الأمر — لم يُبنَ، بصراحة تامة لماذا
فحصت: `parents.phone` مستقل عن هوية `auth.users` (تسجيل الدخول Email OTP الآن، لا Phone Auth).
لكن **لم أبنِ واجهة تعديل فعلية هذه الجولة** — نفدَ الوقت قبل الوصول لهذا البند تحديدًا ضمن
الدرج الكامل غير المبني أصلًا (البند 7). لو بُني لاحقًا: `normalizeSaudiStoredPhoneInput()`
هي الدالة الصحيحة الوحيدة (تحققت من `phone.ts` — موجودة وصالحة تمامًا لهذا الاستخدام).

## 9) منح/إلغاء التعويض اليدوي
`POST /api/admin/makeup-credits`: `grant` يُنشئ صفًا `source_type='manual_admin'`
`status='available'`، يتطلب سببًا + طفلًا. `cancel` يقبل فقط صفوفًا `available` **و**
`manual_admin` تحديدًا — لا يمكن إلغاء رصيد نشأ فعليًا من غياب/إلغاء معلم من هذه الواجهة، حماية
من فقدان حق مكتسَب فعليًا للطالب.

## 10) هل تغيَّرت سياسة انتهاء التعويض؟ لا
تحققت من `policies.ts`: لا سياسة انتهاء معتمَدة لخُطى حاليًا. `expires_at` بقي nullable تمامًا،
يُضبَط فقط إن اختاره الأدمن صراحةً بنموذج المنح — **صفر انتهاء تلقائي مُخترَع**. اكتشفت أيضًا
(دون تغيير أي شيء) أن `manual_admin` مُعفى أصلًا من السقف الشهري (`isMonthlyCapApplicable`
تُعيد `false` له) — استُخدِم هذا الإعفاء الموجود فعلًا، لم أُنشئه.

## 11) Drawer Modules — **لم تُبنَ هذه الجولة**
القرار الصريح الأهم بهذا التقرير: نظام الـDrawer القابل لإعادة الاستخدام (Parents/
Students/Teachers) لم يُبنَ. البديل الآمن: صفحات القوائم (parents/students) من V1 تبقى تعمل
بحثًا وترقيمًا صفحيًا حقيقيَّين، لكن **بلا Quick View**. هذا أكبر Blocker متبقٍّ قبل الإطلاق
الفعلي حسب معايير النجاح المذكورة بالطلب.

## 12) الإعدادات — ما يُستهلَك فعليًا مقابل الزخرفي
**بصراحة تامة: لم أصل لربط أي إعداد بمنطق التشغيل الفعلي هذه الجولة.** `platform_settings`
migration من V1 **لا تزال غير مُطبَّقة**، وصفحة `/admin/settings` (من V1) تحفظ قيمًا **لا
يقرأها أي كود تشغيلي آخر حاليًا** — بالضبط التحذير الذي حذَّرتَ منه صراحةً ("Do NOT ship an
Admin Settings page that saves values the rest of KHOTA ignores"). هذا **Blocker حقيقي** غير
محلول، مذكور بصراحة لا مُغطًّى.

## 13-16) ما لم يُنفَّذ (بصراحة، لا تبرير)
- **AdminShell موحَّد بالكامل**: فقط `teachers.tsx` لُفَّت هذه الجولة (نموذج مُثبَت للنمط).
  `groups.tsx`، `subscriptions.tsx`، `teacher-applications.tsx` **لا تزال بالـShell القديم** —
  قرار متعمَّد لتفادي كسر صفحات معقَّدة تعمل فعليًا تحت ضغط وقت (خاصة بعد أن كسرت JSX بجولة
  سابقة عبر `str_replace` غير دقيق — لن أكرر هذا الخطر هنا).
- **إنشاء/نقل مجموعات** (البند 14): لم يُبنَ إطلاقًا. Blocker معماري حقيقي كما طلبتَ توثيقه:
  نقل طالب بين مجموعتين يتطلب نموذج "سجل قيد تاريخي" (enrollment history) لا يوجد حاليًا —
  `subscriptions` صف واحد لكل اشتراك بربط مباشر بمجموعة، فنقله يُعيد كتابة التاريخ فعليًا. غير
  آمن بدون إعادة تصميم حقيقية.
- **CSV Export**: لم يُبنَ.
- **Drawer/Quick View**: لم يُبنَ (أعلاه).
- **سبب أدير عدم إزالة "قيد التطوير"**: بالنظر لعدم اكتمال البنود أعلاه، لم أراجع القائمة
  الجانبية لإزالة أي رابط — تركتها كما استقرَّت بـV1.

## 17) الأمان
كل mutation جديد (`sessions`، `makeup-credits`) يمرّ عبر `requireAdmin()` المركزي (401/403).
لا معرّف من العميل يُوثَق دون تحقق DB أولًا (الجلسة، المعلم، الطفل، رصيد التعويض كلها تُجلَب
وتُتحقَّق قبل أي كتابة). لم أُشغِّل اختبارات أمان حيّة — لا بيئة تشغيل هنا.

## 18-20) الاختبارات
`npm install`: **فشل** (403، لا وصول شبكة، نفس القيد الثابت من كل هذا المشروع دون استثناء).
`npm run lint`: **فشل** — `eslint: not found` (نتيجة مباشرة لفشل التثبيت).
`npx tsc --noEmit`: نُفِّذ فعليًا. لقيت 7 أخطاء implicit-any حقيقية (بعضها بكود V1 موجود مسبقًا
بملف عدَّلته، وبعضها بكودي الجديد) وصلحتها جميعًا، صفر خطأ متبقٍ بالملفات التسعة.
`npm run build`: **فشل** — `next: not found`، نفس السبب.

## 21) Migration مطلوب تطبيقها قبل النشر
1. `20260917_platform_settings.sql` (من V1، لم تُطبَّق بعد).
2. `20260918_grade_exact_and_action_reason.sql` (هذه الجولة).
كلاهما إضافي بحت، آمن التطبيق على بيانات حالية.

## 22) Blockers الحقيقية المتبقية (بالأولوية)
1. **الإعدادات زخرفية بالكامل** — صفر ربط بمنطق تشغيلي فعلي.
2. **لا Drawer/Quick View** — التفتيش السريع المطلوب بمعايير النجاح غير موجود.
3. **3 صفحات لا تزال بالـShell القديم** — التجربة ليست موحَّدة فعليًا بعد.
4. **إنشاء/نقل مجموعات غير موجود** — موثَّق كحاجز معماري حقيقي، لا محاولة تحايل.
5. **CSV export غير موجود.**
6. **تعديل جوال ولي الأمر غير موجود.**

**لا نشر، لا migration مُطبَّقة تلقائيًا — كما طُلب بدقة.**
